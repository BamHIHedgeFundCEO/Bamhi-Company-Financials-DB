# 分階段執行計畫

> **這份檔案是階段之間的唯一交接文件。**
> 每做完一個階段就可以清掉對話（`/clear`），下一次從這份檔案接手，不需要回溯歷史訊息。
> 策略層面的判斷原則見 `ROADMAP.md`；這份只講「做什麼、資料從哪來、怎麼驗收」。

---

## 產品終局

使用者輸入代碼 →

1. **先落地在一個摘要頁**（網頁直接看，快速判斷業務）
2. 想深入 → **下載 Excel**（現有的 8 分頁活頁簿）

摘要頁的原則：**只放好抓又重要的資料，不會出現 n/a。**
達不到這個標準的欄位不進摘要頁，留在 Excel 裡誠實寫 n/a。

參考對象：`sec-insider.up.railway.app`（概覽 / 損益表及現金流 / 營收細分 / 對沖基金動向）。

---

## 已驗證的事實（2026-08-20 實測，不要再重驗）

### yfinance 可用，且參考站的股價區塊就是它做的

```
period=1y, interval=1d, auto_adjust=True, 0.5 秒抓完三檔
AAPL   225.18 -> 316.83   +40.7%
^GSPC 6395.78 -> 7707.98  +20.5%   → 差 +20.2pp（參考站寫 vs 標普 +19pp）
^NDX  23249.6 -> 29426.0  +26.6%   → 差 +14.1pp（參考站寫 vs 納指100 +14pp）✓ 完全命中
```

本機 yfinance 1.2.0 正常。**風險在 Vercel：Yahoo 常擋機房 IP。**
→ 價格來源必須寫成可換源介面，備案 `stooq.com` CSV（免金鑰、機房 IP 可用）。

### 公司檔案：`submissions.json` 免費附送，零額外請求

有值：`name` / `tickers` / `exchanges` / `sic` / `sicDescription` / `entityType` /
`category`(申報人規模) / `stateOfIncorporation` / `fiscalYearEnd` / `ein` / `phone` /
`addresses` / `formerNames`（含改名歷史與生效日）

**永遠是空字串，別指望：`website`、`description`。**
→ 業務散文只能來自 Item 1 Business 的 HTML（屬 D 階段）。

### Form 4 是純 XML，品質最好的一塊，且一份資料兩用

`https://www.sec.gov/Archives/edgar/data/{cik}/{accession_nodash}/form4.xml`
（`primaryDocument` 欄位會寫成 `xslF345X06/form4.xml`，**去掉 `xslF345X06/` 前綴**才是原始 XML；
帶前綴的是 SEC 的 XSL 轉譯 HTML 版）

實測 AAPL `0001140361-26-032884` 的欄位：

```
rptOwnerName    = Newstead Jennifer
isOfficer       = true
officerTitle    = SVP, GC and Secretary
transactionCode = S      transactionAcquiredDisposedCode = D
transactionShares = 1439   transactionPricePerShare = 307.75
sharesOwnedFollowingTransaction = 40107
aff10b5One      = true     ← 10b5-1 預設交易計畫，「結構化欄位」不是註腳
```

AAPL 光是 `filings.recent` 裡就有 587 筆 Form 4。

**判讀規則（必須內建，否則做出來會誤導）：**
多數 Form 4 是 `A`（股權獎勵）與 `M`→`S`（行權後依 10b5-1 自動賣出）——**那不是看空信號**。
真訊號是 `P`（公開市場買入）與 `aff10b5One=false` 的 `S`。
UI 必須把兩類分開，不可混算成「本季內部人賣出 $X」。

**副產品：高管名單不必另做。** `officerTitle` 就是高管名冊，還自帶持股與變動。
（原本想抓的「歷史創始人」不在 SEC，已改成高管名單。）

### companyfacts 不含任何自訂命名空間

實測 AAPL `['dei','us-gaap']`、TSLA/BRK.A `['dei','ffd','us-gaap']`、NVDA、JPM 同。
→ 「用 companyfacts 建自訂標籤對照表」不是難，是**不可能**。
自訂標籤只能從 XBRL instance 或 DERA Financial Statement Data Sets 取得。

### 全公司標籤盤點只能靠 DERA Financial Statement Data Sets

`https://www.sec.gov/files/dera/data/financial-statement-data-sets/2025q3.zip` = 127,842,298 B
內含 `sub.txt`(6,541 申報 / 5,909 CIK)、`num.txt`(542MB)、`pre.txt`、`tag.txt`(83,782 標籤：標準 8,277 / 自訂 75,505)

**陷阱：`tag.txt` 的 `version` 欄位對自訂標籤而言，是「定義該標籤的申報書號」，
前綴是申報代理商的號碼、不是公司 CIK。**用前綴比對算每家公司的自訂標籤會全錯
（實測 NVDA 會算成 1、TSLA 會算成 0）。
正確做法是三表 join：`num.adsh` → `sub.cik`，再用 `tag.custom='1'` 過濾。
正確結果：JPM 20 / TSLA 18 / AAPL 3 / NVDA 1，**且沒有一個是營收標籤**
（SEC DQC 規則會逼申報人在主要報表上用標準標籤）。

---

## LLM 決策：不用 LLM，也不需要

原本以為 C/D 階段（CAMs / MD&A / 業務概況 / 指引展望）非 LLM 不可。不是。

**這四項的原文本來就是人寫的散文，直接原文直出 + 錨點 + EDGAR 連結即可。**
LLM 唯一真正的用途是「翻成中文並濃縮」，那是加值層不是必要層，而且它正好是
唯一會違反「資料一定是正確」的一層。

**定案：**

- 主線一律 **零 LLM、零 API 成本**：抽出原文段落，附 `Item 1A` 之類的來源標籤與 EDGAR 連結
  （參考站那個小標籤做法是對的，抄它）
- 若日後真的要中文摘要 → **離線批次跑在自己機器上**（本機模型或手動），產物存成 JSON
  提交進 repo / Blob，執行期零成本、零延遲、可人工覆核
- **數字層永遠不經過 LLM。** 這條不可協商

---

## 階段表

| # | 項目 | 狀態 | 破規則? | LLM? |
|---|---|---|---|---|
| 0 | FSDS 覆蓋率盤點工具 | **完成** `tools/fsds_coverage.py` | 否 | 否 |
| A | 摘要頁：損益 / 現金流 / 營收細分 | 待做 | 否 | 否 |
| A+ | 公司檔案 + 股價 vs 指數 | 待做 | 否 | 否 |
| B | 13F 對沖基金動向 | 待做 | 否 | 否 |
| B+ | Form 4 內部人買賣 + 高管名單 | 待做 | 否 | 否 |
| C | CAMs | 待做 | **是** | 否 |
| D | MD&A + 指引展望 + 業務概況散文 | 待做 | **是** | 否 |
| E | 台股 | 待做 | — | — |

0 / A / A+ / B / B+ **不用改規則、不用 LLM、數字可驗**，五項蓋掉參考站除了
「未來發展」「主要風險」以外的每一塊。

---

### 階段 0 — FSDS 覆蓋率盤點工具

**為什麼排第一：** 它的產出直接決定階段 A 摘要頁能放哪些欄位。
「不會有 n/a」不能用猜的，要有全市場密度數據支撐。

**做什麼：** `tools/fsds_coverage.py`
吃一包 FSDS 季度 zip，離線盤點 `config/xbrl_zh_map.json` 在全部 5,909 家上的覆蓋率。
**per-company 零 SEC 請求。**

**要回答的問題：**
1. 每個科目的全市場命中率（有多少家真的抓得到）
2. 依 SIC 產業分組的命中率 → 哪個科目在哪個產業整片抓不到
3. 抓不到的那些公司實際用了什麼標籤（候選補進 alias 清單）
4. 哪些科目密度夠高，可以進摘要頁而保證不出現 n/a

**必須避開的坑：**
- `version` 前綴比對 → 錯，要三表 join（見上）
- 只看 `num.txt` 有沒有該 tag 是不夠的：管線還有維度過濾與期間長度過濾，
  掃描要模擬管線的 fallback 層級，否則假陰性滿天飛（`coverage-sweep-false-negatives`）
- `num.txt` 542MB → 串流讀，不要整包進記憶體

**驗收：** 產出一份分產業的缺口報告；抽三個報告說「抓不到」的公司，
用 `tools/coverage.py` 打真 SEC 驗證確實抓不到。

#### 已完成（2026-08-20，commit `ea74e97`）

`python tools/fsds_coverage.py 2025q3.zip`（可餵多包合併）

**產出的第一個修法：**現金淨變動補上 `…PeriodIncreaseDecreaseExcludingExchangeRateEffect`
→ 全市場命中率 **76.5% → 98.7%**，map v1.5 → v1.6。

**驗證缺口的正確問法（踩過才知道）：**
- 問「這家歷史上用過我們收的標籤嗎」→ 假警報率 **87%**
- 問「這家在**那幾期**用了嗎」→ 假警報率 **3%**

網站呈現的是特定期間，所以缺口的單位是「**期**」不是「家」。
比對時要用 FSDS 的 `ddate` 對 companyfacts 的 `end`。

**通用摘要頁（所有產業都 ≥90%）目前 6 個：**
營業活動現金流量、本期淨利、資產總計、負債總計、現金淨變動、籌資活動現金流量

**分產業可上摘要頁的欄位數：**其他製造業 20 / 生技製藥 19 / 其他服務業 17 /
銀行與信貸 15 / 控股與投資機構 8

→ **摘要頁必須分產業樣板**，這點已由數據證實，不是猜的。

**低分 ≠ map 壞掉。**生技製藥「非利息收入」0%、軟體業「存貨」低，都是正確的
——那種格子該顯示「—」（不適用）不是 n/a。工具的第二部分才是要動手修的清單。

**尚未處理的候選（第二部分還有）：** `AccountsPayableAndAccruedLiabilitiesCurrentAndNoncurrent`
(309 家)、`DeferredRevenue` / `DeferredRevenueNoncurrent`(96 家)。
`CommonStockSharesIssued` 雖然分數最高**但絕對不能加**——SharesIssued 含庫藏股，
會讓市值高估。

#### 衍生成果：三大報表的「—」（commit `40ca8b1`）

覆蓋率盤點的第一個直接應用。`config/concept_applicability.json`（21 個 SIC 產業組）
由 `fsds_coverage.py --applicability` 產生，執行期 `web/server/utils/applicability.ts`
判斷缺值該寫 `n/a` 還是 `—`。SIC 由 `resolveCompany` 從 submissions 帶出，零額外 SEC 請求。

| | 舊 n/a 率 | 新 n/a 率 |
|---|---|---|
| AAPL | 6.4% | **4.7%** |
| REGN | 8.9% | **7.0%** |
| JPM | 19.0% | **8.0%** |
| BRK.A | 23.8% | **18.1%** |

四家的「有值」格數逐字不變 —— 沒有任何真數字被蓋掉。

**已知限制：**BRK.A 降幅小是 SIC 粒度的問題。它是 6331（保險）但實際像控股公司、
用未分類資產負債表；同組多數小型保險公司有分流動／非流動，所以「流動資產合計」
在保險組沒低於門檻。→ 已由下一節的逐家層解掉。

#### 逐家適用性（取代產業層當第一順位）

產業表判不出同業裡的異類，所以再做一層**逐家**：問這家公司自己的報表
（FSDS `pre.txt` 的 IS/BS/CF）上有沒有語意相當的那一行。有 → 真缺口，維持 `n/a`；
沒有 → 不適用，寫「—」。產物 `config/company_applicability.json`（211 KB，7,093 家）。

執行期兩層，**逐家優先、不跟產業表取聯集**（衝突時逐家才對，取聯集等於讓差的那份也有票）；
逐家沒收錄才退回產業表，兩層都沒有就維持 `n/a`。

資料範圍：**四季 FSDS 聯集**（2025q3/q4、2026q1/q2）。一季只有 10-Q（20-F 僅 94 家），
2026q1 那季有 4,262 份 10-K + 372 份 20-F，補上外國發行人與非曆年公司。
一季 5,542 家 → 四季 7,202 家。

三個和產業層不同的關鍵設定，每個都是踩過才知道：

1. **自訂命名空間也算證據。** 覆蓋率報告只看 us-gaap/ifrs/srt，但適用性問的是
   「這一行存不存在」。公司用自訂標籤報存貨，那一行就存在，而且 companyfacts
   抓不到它 —— 那是**真缺口**，要留 n/a。判成不適用會把洞藏起來。
2. **多季 union。** 10-Q 比 10-K 短，只看一季會把年報才有的行判成不適用。
3. **看不清楚就不判。** 三張表任一張看到的標籤數 < 8 就整家跳過（107 家），
   退回產業表。沒申報過的公司完全不會出現，不會被判成「什麼都不適用」。

**執行期護欄（`financials.ts`）：只要該科目在任何一期抓到過值，就一定適用。**
適用性表是一家公司一個旗標、不分期間，而它判的是「有沒有出現在報表正面」。
蘋果與波克夏的使用權資產只在租賃附註、不在資產負債表正面 → 被判不適用，
但 companyfacts 抓得到、20 期有數字。沒這道護欄同一列會同時出現數字和「—」。

| | 原始 | 產業層 | 逐家層 | 「—」 |
|---|---|---|---|---|
| AAPL | 6.4% | 4.7% | **4.7%** | 95 |
| REGN | 8.9% | 7.0% | **8.0%** | 22 |
| JPM | 19.0% | 8.0% | **9.1%** | 339 |
| BRK.A | 23.8% | 18.1% | **18.9%** | 132 |

**n/a 率比產業層略升是預期的** —— 逐家層把產業層猜錯的「—」退回 n/a（BRK.A 的
營業成本／毛利、JPM 的應付帳款／應收帳款，那些公司報表上真的有那一行）。
四家有值格數逐字不變，且沒有任何一列同時出現數字與「—」。

**BRK.A 已修好的部分：**流動資產合計／流動負債合計／短期借款／營業利益四項
由 `n/a` 改為「—」（未分類資產負債表 + 無營業利益小計），這是產業層做不到的。

##### 誤判稽核（`--audit-na`）

誤判成「不適用」是唯一會說謊的方向，所以做了一支專門攤開它的稽核：列出所有
「判成不適用、但該公司報表上有分數 1.5–3.0 的標籤」的組合。第一次跑抓到真問題：

> **`Cash`（491 家）與 `CashAndDueFromBanks`（52 家）不在 map 裡。**
> 877 家的資產負債表只用這兩個標籤（US Bancorp、M&T、AIG、Loews、American Airlines
> 都在內）。隨機抽 14 家做**期間比對**，6 家有只靠這兩個標籤才拿得到的期
> （Cathay General 30 期）→ 會被寫成假的「—」。已補進 map v1.7，補完後
> 「現金」整條從稽核清單消失。

稽核清單剩下的都確認是不同的東西（par value ≠ 庫藏股、銀行薪資 ≠ SG&A、
折舊 ≠ 營業成本、應付稅款 ≠ 短期借款），維持不適用。

##### 獨立交叉驗證

用 `num.txt` 的同業實際申報率（跟 `pre.txt` 詞元完全不同的訊號）檢查 36,657 個
「判不適用」組合：

| 同業申報率 | 組合數 | 佔比 |
|---|---|---|
| < 15%（強烈支持） | 13,953 | 38.1% |
| 15–50% | 14,902 | 40.7% |
| 50–85% | 6,229 | 17.0% |
| **≥ 85%（可疑）** | **1,573** | **4.3%** |

可疑的 4.3% 抽 16 個查 companyfacts：10 個被執行期護欄擋下，6 個真的會寫「—」，
而那 6 個逐一查證**全部是對的**：

- Mettler-Toledo、Corteva、Fluence Energy 的損益表確實沒有營業利益小計
  （Fluence 整份 companyfacts 沒有 `OperatingIncomeLoss`）
- Titan Machinery 用單一 `OperatingExpenses`（195 筆）不拆 SG&A
- Canterbury Park、Granite Point 的費用結構裡沒有 SG&A 那一行

也就是說「可疑」桶裡錯的是產業統計，不是逐家判定 —— 這正是做逐家層的目的。

##### 兩個試過但更糟的收緊法（不要再試）

`NA_MIN_SCORE = 3.0` 是**刻意調寬**的，寬到有已知偽陽性。收緊過兩次都更糟：

| 做法 | 動機 | 結果 |
|---|---|---|
| 核心詞（60% 標籤共有且 idf ≥ 2.0） | `income`+`expense` = 3.44 就過門檻，`NonoperatingIncomeExpense` 被當成蘋果有申報淨利息收入 | 長期負債判不適用 85 → **2,743 家**（`NotesPayableNoncurrent` 沒有 long/term）、合約負債 871 → 4,501 |
| 贏面（不到最高分的 1/1.25 就不算） | `IncomeLossFromContinuingOperationsBefore...NoncontrollingInterest` 含 interest | 營業收入判不適用 59 → **923 家** |

兩者都是拿偽陰性換偽陽性，方向反了。偽陽性只讓格子留在 n/a（難看但誠實）；
偽陰性把真缺口寫成「—」（說謊）。

##### 第三層：結構性否決（structural，唯一可以蓋過逐家的東西）

上面說的「蘋果淨利息收入顯示 n/a」不是個案，是**系統性**的：逐家判斷靠標籤詞元
比對，而「淨利息收入」的詞元全是泛用字（interest／income／expense），幾乎每張
損益表都有含 `income`+`expense` 的標籤。實測 7,093 家裡只有 **110 家**被判不適用
—— 整片非金融業的淨利息收入都在顯示 n/a，佔 18 家抽樣殘餘 n/a 的 16%。

先試過從詞元下手，兩條都不划算（實測數字）：

| 做法 | 淨利息收入判不適用 | 副作用 |
|---|---|---|
| 去除 `NoncontrollingInterest` 再斷詞 | 110 → 124 | 幾乎沒用 |
| 再加「重疊詞不能只有 income/expense」 | 110 → 1,136 | 所得稅費用 316 → **1,191**、利息費用 76 → **599** |

改用產業統計否決。`concept_applicability.json` 因此有兩份清單：

- `not_applicable`（同業申報率 <15%）—— **只在逐家表沒收錄該公司時當退路**
- `structural`（<8%）—— **一律與逐家判斷取聯集**

門檻 8% 不是隨便挑的，是逐一查證翻面結果選出來的：

| 門檻 | 18 家抽樣 n/a 率 | 翻面內容 |
|---|---|---|
| 現況（無否決） | 11.3% | |
| <8% | 8.7% | 23 個翻面**全對**（非金融業的淨利息收入、券商的營業成本／毛利、銀行與 REIT 的研發費用） |
| <15% | 7.2% | 開始說謊：JPM／美銀的**應收帳款**、波克夏的**營業成本與毛利** —— 那些公司報表上真的有那一行 |

產出的 structural 清單很緊，證明門檻對：銀行組只有存貨／研發費用／合約負債，
**應收帳款與應付帳款沒被否決**（JPM 的真缺口留著）；保險組沒有營業成本／毛利
（波克夏的留著）。

##### 三層合起來的實測（18 家跨產業，40 季）

| | n/a | 「—」 | 有值 | n/a 率 |
|---|---|---|---|---|
| 逐家層 | 1,629 | 1,321 | 12,850 | 11.3% |
| **＋結構性否決** | **1,281** | **1,669** | 12,850 | **9.1%** |

有值格數逐字不變，沒有任何一列同時出現數字與「—」。

**所以「Excel 沒有 n/a 了嗎」的答案是：沒有，而且不該沒有。** n/a 的意思是
「這格該有數字、我們沒抓到，你去查」，把它清成 0 或「—」才是壞掉。目前 9.1%。

##### 殘餘 n/a 集中在哪（下一批要修的）

| 公司 | n/a 率 | 主要來源 |
|---|---|---|
| BRK.A | 34.4% | 營業成本／毛利／銷售及管理費用（波克夏損益表有 cost of sales，我們沒對照到） |
| XOM | 20.1% | 營業成本／毛利／研發費用（油氣業用 crude oil and product purchases 之類的標籤） |
| PLD | 16.2% | 營業成本／毛利（REIT 的費用結構） |
| JPM／BAC | 14.4%／10.3% | 應收帳款／應付帳款／短期投資（銀行版標籤，`--audit-na` 已列為候選） |

共同型態是**非典型費用結構的營業成本**。這是下一輪 `--audit-na` ＋期間比對的題目。

#### 真缺口普查（`--na-gaps`，map v1.8）

`--audit-na` 查的是反方向（判不適用但可能有標籤）。要修真缺口需要正方向的普查：
把三層適用性套進來模擬產品實際渲染，只留下真的會顯示 n/a 的格子，再問
「這家公司報表上有哪個我們沒對照的標籤」，按可修家數排名。

全市場 271,004 個科目格子（6,181 家 × 2026q1+q2）裡有 42,209 個顯示 n/a（15.6%），
拆開之後結論和直覺完全不同：

| 類別 | 格數 | 意義 |
|---|---|---|
| 可補標籤 | 6,658 | 補 map 就會有值 —— **只有這 16% 是真的錯誤** |
| 僅維度 | 3,795 | 標籤只有帶維度的版本，companyfacts 看不到，n/a 正確 |
| 自訂標籤 | 4,537 | 公司用自訂命名空間報，companyfacts 整個剝掉，n/a 正確 |
| 判太寬 | 27,219 | 適用性放太寬，這格其實該顯示「—」（**64.5%**） |

「蘋果的淨利息收入該是「—」卻顯示 n/a」不是個案，是**近三分之二殘餘 n/a 的型態**。

##### 兩道閘門，缺一個就滿是噪音

單靠標籤詞元比對會產出這種候選：`DeferredIncomeTaxLiabilitiesNet`（遞延所得稅）
被指給「合約負債」1,033 家、`ProceedsFromSaleOfPropertyPlantAndEquipment`（處分收入、
方向相反）被指給「資本支出」95 家。兩個都是被 `deferred`／`property plant equipment`
這種共同詞帶過門檻。

加上**行標題閘門**（pre.txt 的 `plabel`＝申報人自己印在報表上的那行字）之後，
資本支出候選 848→488、應收帳款 619→269、所得稅 138→43。標籤名被分類法的命名慣例
帶偏，行標題被公司自己的簡寫帶偏，兩者錯的地方不一樣，取交集才留得下真的。

##### 驗證方法：期間比對，不是看名字像不像

對每個候選打 live companyfacts，在**兩邊都有值的期**上比數值。真同義詞會相等或呈
穩定倍數，不同一條的會亂。五個故意放進去的對照組全部被擋下：

| 對照組（已知錯） | 重疊期相等 | 中位倍數 |
|---|---|---|
| `LiabilitiesAndStockholdersEquity` → 股東權益 | 0 / 34 | 6.18 |
| `ProceedsFromSaleOfPropertyPlantAndEquipment` → 資本支出 | 0 / 140 | 0.03 |
| `AccretionAmortizationOfDiscountsAndPremiumsInvestments` → 折舊攤銷 | 0 / 120 | **−0.57** |
| `AccruedLiabilitiesCurrent` → 應付帳款 | 0 / 155 | 6.00 |
| `DeferredIncomeTaxLiabilitiesNet` → 合約負債 | 0 / 60 | 2.69 |

也擋下兩個看起來很合理的：`PaymentsToDevelopRealEstateAssets`（0.14）與
`PaymentsToAcquireOtherProductiveAssets`（0.31）只是資本支出的**子項**，收進來會把
REIT 的資本支出少報八成 —— 錯的數字比 n/a 更糟。

##### ⚠ 最大的陷阱：map 的 tags 會餵進適用性的 idf

**在 A 科目加一個 alias，會靜靜地改掉 B 科目對幾百家公司的判定，方向還是說謊的那一邊。**
這件事在覆蓋率、命中率、n/a 總數上全都看不出來（總數還會變好看，因為「—」不算 n/a）。

實測兩次，都是為了修個位數家數而弄壞上千格：

| 加的標籤 | 修 | 賠 | 機制 |
|---|---|---|---|
| `LimitedLiabilityCompanyLlcMembersEquity…` | 5 家 | **1,622 格** | `LimitedLiability` 貢獻 `liability` 一詞 → 其 idf 3.22→2.81 → 靠 {liability, lease}／{liability, deferred} 得分的標籤整批跌破 3.0 → 長期租賃負債 808 家、合約負債 814 家從 n/a 翻成「—」，抽 10 家有 4–5 家其實有申報 |
| `DebtSecuritiesAvailableForSaleExcludingAccruedInterestCurrent` | 52 家 | **957 家** | 把 debt／securities／available／sale／accrued 灌進「短期投資」的詞彙 → 銀行整批投資證券標籤變成「有這一行」的證據 → 原本正確的「—」翻回 n/a |

兩個都撤掉了。`--company-applicability` 現在會**自動比對現有那份並印出新增的「不適用」格數**；
新增＝原本 n/a 改寫「—」＝唯一會說謊的方向，每個科目抽 10 家打 companyfacts 確認。
撤掉之後這個警告歸零。

##### v1.8 的實測結果

補進 map 的 15 個標籤全部通過期間比對（應付帳款合併行 3 個、資本支出 2 個、
利息費用 2 個、PP&E 2 個、應收帳款／折舊攤銷／短期投資／合約負債各 1 個）。

| | n/a | 「—」 | 有值 | n/a 率 |
|---|---|---|---|---|
| v1.7（當場從 HEAD 重跑） | 2,939 | 2,956 | 24,905 | 10.6% |
| **v1.8** | **2,869** | 2,923 | **25,008** | **10.3%** |

18 家跨產業、每家 33–37 期。**有值格數 +103，沒有任何一家倒退**，
JPM 15.2%→12.6%、PLD 16.5%→13.7%。不變式仍成立（沒有任何一列同時有數字與「—」）。
全市場普查 n/a 42,209→41,601，最大單項是應付帳款 −516。

##### 下一步的優先順序（已量化）

1. **判太寬那 27,219 格**（64.5%）—— 最大的一塊。已做，見下。
2. 可補標籤的長尾 —— 剩下的候選每個只修 2–50 家，投報率遞減。
3. 僅維度 3,795 + 自訂標籤 4,720 —— **不要修**，companyfacts 結構上就抓不到，
   n/a 是正確答案。

#### 行標題撤銷（`build_evidence` 的第二道閘門）

適用性的證據原本只看標籤詞元，所以 `OperatingLeaseLiabilityNoncurrent` 會因為
{liability, lease} 被當成「這家公司有長期負債那一行」的證據 —— 真的沒有的公司也
維持 n/a。修法**不是**把詞元門檻調嚴（試過兩次都反效果），而是引進獨立訊號：
申報人自己印在報表上的那行字。規則只往單一方向作用：

| 行標題 | 動作 |
|---|---|
| 明確指名**別的**科目 | 撤銷這條證據 |
| 對這個科目也夠分 | 留著（兩個訊號一致） |
| 對每個科目都不夠分（"Other"、"Total"、公司簡寫） | **留著** —— 含糊不代表沒有 |

第三條是安全閥，少了它 terse label 的公司會整片被誤判。直接對照（標籤就在 map 的
`tags` 裡）不受閘門影響 —— 公司把 Inventory 那行叫什麼都不改變它是存貨。

##### 查證：4,088 格翻面、0 格收回，抽樣全對

單向收緊（只新增不收回）本身就是設計正確的訊號。抽 8 家 × 6 個科目把**被撤銷的證據
原文**印出來人工判，全部撤得對：

| 科目 | 被撤銷的證據（標籤 → 申報人寫的行標題） |
|---|---|
| 商譽 | `IntangibleAssetsNetExcludingGoodwill` →「Intangible assets, net」（標籤字面就寫排除商譽） |
| 合約負債 | `OperatingLeaseLiabilityNoncurrent` →「Operating lease liabilities」 |
| 研發費用 | `OrganizationalCostExpense` →「Organization costs」（BDC 的開辦費） |
| 所得稅費用 | `InterestIncomeExpenseNet` →「Interest income」 |
| 淨利息收入 | `IncomeTaxExpenseBenefit` →「Provision for income taxes」（EPAM 這類軟體公司） |

實測：全市場 n/a 41,601 → **38,812**（−2,789，−6.7%），判太寬 27,114 → 24,428。
18 家跨產業抽樣 10.3% → 10.2%（只有 REGN 動了 13.2%→11.3%）—— 大型股的證據多半來自
直接對照，閘門碰不到，市場層級的數字才是這一步的成效。不變式仍成立。

##### 行標題訊號的上限（量過了，不要再往這條路推）

蘋果那格之所以推不動，是因為 `NonoperatingIncomeExpense` 的行標題
「Other income/(expense), net」剝掉停用詞只剩 `{income, expense}`，對營業收入／
淨利息收入／所得稅費用／利息費用**同分 3.44** —— 四個都指名等於誰都沒指名。
**申報人自己那行字就沒說**，這不是門檻調得不夠好。要靠它翻過去只能改成
「含糊就撤銷」，那是說謊方向。

（蘋果的淨利息收入本身早就由第三層結構性否決處理成「—」了，電腦硬體組在
`structural` 清單裡。上面講的是這個型態的一般情形。）

剩下的 24,428 格判太寬集中在長期負債 2,382、淨利息收入 2,047、短期投資 1,768、
應收帳款 1,605、短期借款 1,575，都是行標題含糊而安全閥放行的。下一步要換第三個
獨立訊號。

#### 第三個訊號：共現＝不同行

前兩個訊號都在問「這個標籤看起來像不像那個科目」。行標題含糊時兩個都答不出來。
第三個訊號問完全不同的問題：**這兩個標籤會不會出現在同一張報表上？**

| | 判定 | 例子 |
|---|---|---|
| 會共現 | **兩行**，不是替代品 -> 撤銷證據資格 | `OperatingLeaseLiabilityNoncurrent` 與長期負債同時出現在幾千張資產負債表上 |
| 不共現 | 替代品 -> 確實是證據 | `CashAndDueFromBanks` 幾乎不與 `CashAndCashEquivalentsAtCarryingValue` 同框（銀行用前者） |

純結構訊號，跟命名慣例、行標題寫法都無關，所以與前兩個獨立。門檻共現率 ≥50%、
觀測 ≥30 家（冷門標籤的共現率是雜訊，觀測不足就不撤銷 —— 一樣是「不知道就不要猜」）。
全市場撤銷 1,392 組 (標籤, 科目)，前幾名一眼就知道對：

| 候選標籤 | 原本被當成 | 共現率 | 家數 |
|---|---|---|---|
| `NetCashProvidedByUsedInOperatingActivities` | 籌資活動現金流量 | 99% | 6,505 |
| `LiabilitiesAndStockholdersEquity` | 股東權益總計 | 98% | 6,471 |
| `EarningsPerShareBasic` | 稀釋每股盈餘 | 97% | 6,032 |
| `CommonStockSharesIssued` | 期末流通股數 | 91% | 6,112 |

##### 查證：11,451 格翻面、0 格收回

三倍於行標題閘門的翻面量，所以查得更兇。先量**真正的傷害**：翻面的列在顯示窗
（近 9 年）內還有沒有值？有值的話執行期護欄會擋住，根本不會顯示「—」。
抽 100 格，49 格被護欄擋住、51 格真的會顯示「—」。對這 51 格逐一看公司報表：

| 公司 | 科目 | 該張報表上的實情 |
|---|---|---|
| Haverty Furniture（家具零售，**真營運公司**） | 應收帳款 | 33 行裡沒有應收帳款 —— 現金／第三方融資銷售 |
| Context Therapeutics、Fermi、兩家 SPAC | 營業收入 | 損益表上沒有營收那一行 |
| **XOM** | 營業成本／毛利 | 埃克森損益表只有 `CostsAndExpenses`「Total costs and other deductions」，沒有毛利小計 |
| **BRK.A** | 稀釋每股盈餘／稀釋股數 | 損益表只有 `EarningsPerShareBasic`「Net earnings per average equivalent」，**沒有稀釋那一行** |
| **WMT** | 無形資產 | 資產負債表沒有單獨的無形資產行，併在其他長期資產 |

全部撤得對。順帶推翻我自己先前的判斷：把 XOM／BRK.A 的營業成本列為「下一批要補
標籤的熱點」是錯的，正確答案是「—」。

##### 實測

| | n/a | 「—」 | 有值 | n/a 率 |
|---|---|---|---|---|
| 兩個訊號 | 2,835 | 2,957 | 25,008 | 10.2% |
| **三個訊號** | **2,631** | 3,161 | 25,008 | **9.5%** |

全市場普查 n/a 38,812 → **28,821（−25.7%）**，判太寬 24,428 → 15,873（−35%）。
有值格數不變，不變式仍成立。XOM 21.8%→16.3%、BRK.A 38.3%→35.3%、WMT 6.5%→4.5%。

三個訊號合起來，殘餘 n/a 從最初的 42,209 降到 28,821（−31.7%），
判太寬從 27,219 降到 15,873（−41.7%）。

---

### 負債總計把非控制權益算進去了（map v1.9）

量「僅維度／自訂標籤」兩桶的可行性時撞到的既有缺陷，跟 n/a 無關 —— 它是**已經在
顯示的錯數字**，而且畫面上完全看不出來，就是個看起來正常的數字。

`Liabilities` 這個標籤很多大公司根本不報（資產負債表上沒有「負債總計」那一行）：
WMT、INTC、DIS、KO、ORCL、CHTR、SRE、HON、ACN 都是，全靠 `derive` 推算。原式是
`total_assets - equity`，而 `equity` 的標籤優先序是 `StockholdersEquity`（**母公司
單獨**）在前 —— 這是對的，ROE 的分母要配母公司淨利 —— 於是非控制權益與可贖回權益
就一起被算進了負債。

#### 實測（2026q1+q2，只取 USD，容差 max(負債 0.5%, 資產 0.2%, $1,000)）

在 **`Liabilities` 有申報**的 15,237 個期上比對，因為只有那裡才驗得出對錯：

| 規則 | 對 | 錯 | 寫錯率 |
|---|---|---|---|
| 現行　`資產 − equity` | 11,612 | 3,625 | **23.79%** |
| 只換權益優先序　`資產 − equity_total` | 13,432 | 1,805 | 11.85% |
| **`資產 − equity_total − temporary_equity?`** | **14,516** | **721** | **4.73%** |

也試過「規則二：沒有含 NCI 的憑據就放棄（寫 n/a）」—— 寫錯率 4.8% 一樣，但放棄了
10,308 個期，其中絕大多數本來就算得對。放棄不是免費的，這裡沒有理由付。

#### 落地

新增兩個 **`internal: true`** 科目（參與推算、不輸出成報表列）：

- `equity_total` 標籤優先序刻意與 `equity` **相反**：先取
  `StockholdersEquityIncludingPortionAttributableToNoncontrollingInterest`
- `temporary_equity` 可贖回特別股／可贖回非控制權益。GAAP 擺在負債與權益之間、
  兩邊都不算。絕大多數公司沒有這一塊，所以在推算式裡寫成選用項 `?`（缺值當 0）。
  `?` 只作用在推算式內部，**不會產生任何顯示的 0**

`derive` 因此從二元擴充成多項左結合（`a - b - c?`）。設定層表達不出「缺值當 0 的
選用項」，這是少數該改程式碼的情形。

⚠️ 內部科目的 tags **不進 `tools/fsds_coverage.py` 的 vocab** —— 它們不是候選對照的
競爭者，混進去只會墊高 `equity`／`interest`／`amount` 的 df、壓低 idf，靜靜翻掉別的
科目上千格適用性判定（見 v1.7→v1.8 那兩次事故）。但 **tag2concepts 要收**，否則工具
會以為那條 derive 從不成立，憑空多報 649 家 n/a —— 模型與管線脫節就是這樣來的。

#### 受影響的公司與驗收

227 家的負債總計會變。前幾名：

| 公司 | 期 | 現行負債 | 修正後 | 高估 |
|---|---|---|---|---|
| INTEL | 2026-03-31 | 93,938M | 80,343M | **13,595M** |
| SEMPRA | 2026-03-31 | 81,279M | 74,044M | 7,235M |
| WALT DISNEY | 2026-03-31 | 96,509M | 89,905M | 6,604M |
| WALMART | 2026-04-30 | 195,277M | 188,632M | 6,059M |
| CHARTER | 2026-03-31 | 138,259M | 133,574M | 4,685M |
| COCA COLA | 2026-03-31 | 70,584M | 68,483M | 2,101M |

驗收：DIS 的差額 6,604M 與 SEC 申報的 `MinorityInterest`（2026-03-28，6,604M）
**分毫不差**；WMT 差額 6,059M 對應 NCI 6,352M ＋ 可贖回 NCI（兩者相抵）。
AAPL 直接有 `Liabilities` 標籤、不走推算，數字不變（對照組）。
`--na-gaps` 普查逐行比對與 v1.8 **完全一致**（除版本字串），確認沒有適用性漂移。

---

### 按股別的股數：離線預算擴到 37 家（2026-08-20）

**母數**。1000 家 universe 的股數三列共 1,198 格 n/a，歸因（FSDS 2025q3–2026q2）：

| 成因 | 格數 | 補得掉？ |
|---|---|---|
| FSDS 完全沒這個標籤 | 589 | ✗ 公司沒申報 |
| 無維度有值（別的原因） | 364 | ✗ 不是這個問題 |
| **只有按股別的維度值** | **242** | ✓ 離線預算 |
| 只有其他維度值 | 3 | ✗ |

（提案裡的 1,705 格是**全體 FSDS 申報人**的數字，那份母體含幾千家不在 universe 的
小公司。實際能落到產品上的是 242 格。）

**做了三件事**：

1. **期末股數與加權平均股數不再連坐**。舊版 `run()` 結尾是 `if not shares: return None`
   —— KKR 的封面股數本來就是無維度的（那列滿的），但加權平均掛了
   `ClassOfStock=CommonStock` 維度整個消失，兩列 36 格全空，卻因為「期末算不出來」
   被整家丟掉。
2. **申報視窗 8 → 16 份**。既有 22 家的歷史只到兩年前，更早的期照樣 n/a
   （UHAL FY2023 的加權平均是錯的 1,961 萬 —— 只抓到投票權那一個股別）。
3. **新增 15 家**：KKR、BAM、TSN、CWEN、TEM、TKO、ARES、BKR、CNM、WSO、VNOM、
   VSNT、GLIBK、LLYVK、MDLN。

| | 舊 | 新 |
|---|---|---|
| 公司 | 22 | **37** |
| 日期點 | 718 | **1,406** |
| 三列有值／總格 | 1,432 / 1,947 | **1,653 / 1,947** |
| 殘餘 n/a | 515 | **294**（−43%） |

**Up-C 的非經濟股別**（判定靠淨利÷EPS，不是硬編碼）：CWEN 剔掉 Class B／D 之後
1.21 億，公司自己的除法說 1.22 億 —— 對得上；照加是 2.02 億，市值會高估 67%。
同一條規則也在 TKO、VNOM、RYAN、UWMC、BROS、ASTS、HLNE、CVNA 上生效。

**新增三道保護**（都是量出來才加的，不是預防性）：

- `drop_basis_outliers()` — 跳出去又跳回來的孤立點丟掉。全檔 1,406 點只命中 4 點
  （CVNA 2025-06-30 那期把 Garcia 家族的 Class B 也標進來了、BAM 2022 年報寫的是
  公開上市那 25%）。**尖點與階梯要分得開**：IPO／分割／併購換股是階梯（跳上去就
  留在上面），所以中間點要求同時偏離前後兩鄰居、且前後兩鄰居彼此接近。
- `one_basis()` — 相鄰跳 4 倍以上、而且沒還原過分割 → 整條數列作廢。命中 CVNA 的
  加權平均（2026 年 5 股換 1 股，`StockholdersEquityNoteStockSplitConversionRatio1`
  不在這次的申報視窗裡）與 RKT 的基本股數。作廢後退回 companyfacts，
  就是沒有預算檔時的樣子。
- `--merge` 旗標 —— 不帶它就是**整檔取代**：只列兩家去跑，其餘 35 家會從檔案裡
  消失，表現出來是那些公司股數整欄變 n/a，沒有任何錯誤訊息。

**否決掉的一條規則**：把期末股數那道既有的「只留與最新一筆同量級（0.5–2 倍）」
套到加權平均上。實測會丟掉 669 點中的 83 點（12.4%），而 ASTS、MSTR、RDDT、
CVNA 那些是**真的成長／增發**，五年內漲 4–5 倍很正常。期末股數那條數列短又近期，
加權平均跨五年，同一個門檻在兩邊的意義不一樣。

**驗收**：三條獨立路徑，兩兩對照——(A) 工具走 XBRL instance、(B) FSDS `num.txt`
的逐股別值、(C) 公司自己的除法（淨利÷EPS）。再加產品層的兩道檢查：列內相鄰不得
跳 1.6 倍（同一列混兩種基礎）、與隱含股數不得差 25%。前後對照修好 24 項、
新增 17 項，新增的逐項都交代得出來：13 項是同一個缺陷因為歷史變長而往前移一季
（修好那側同時消失），2 項是 IPO 當季的假警報（隱含股數是加權平均概念、
期末是時點概念，IPO 那季本來就該差），2 項是 CVNA 的分割污染從預算檔底下
**浮出來**（見下）。

---

### 還沒修：期末股數的基礎錯誤（8 家，直接影響市值）

量法：期末股數 ÷ 加權平均股數的**中位比值**偏離 20% 以上（單期比會被 IPO、
併購那種一次性跳動汙染）。1000 家裡有 8 家：

| ticker | 中位比值 | 症狀 |
|---|---|---|
| EG | 1.80 | `CommonStockSharesOutstanding` 7,450 萬，加權平均 3,880 萬。市值高估 ~90% |
| RKT | 1.57 | Up-C，期末含 Class L、加權平均只有 Class A |
| FOUR | 1.28 | 封面按股別加總 vs 加權平均只有 Class A |
| IP | 1.27 | 期末 6.27 億三期一模一樣，加權平均 5.29 億 |
| LAZ | 1.20 | 封面含可交換單位 |
| PSN | 0.45 | 期末 5,690 萬、加權平均 1.07 億 —— 反方向，市值低估一半 |
| HEI／HEI-A | 0.40 | **同一列**從 5,514 萬跳到 1.39 億：前一期只算了一個股別 |

這些不是多股別維度問題（EG、PSN、IP 根本單一股別），離線預算救不了，
要在 `financials.ts` 裡處理。另外 **CVNA 的 5:1 分割沒被偵測到**，
加權平均整列在 1.22 億 ↔ 6.77 億之間鋸齒；同型的還有 Up-C 的稀釋股數
（UWMC、RYAN、TKO 逐季在「含 LLC 單位」與「只有 Class A」之間跳 10–17 倍）。

---

### 階段 A — 摘要頁

資料**今天全都在** `/api/financials` + `/api/segments`，這是純前端 + 一個 route。

**「簡單的不會有 n/a」要按產業修正：**對非金融業成立，對銀行 / REIT 不成立
（毛利率、營業成本這類概念它們根本不報）。
→ **分產業樣板**，不是一套欄位打天下。欄位清單由階段 0 的密度數據決定。

**UI 尚未討論定案 —— 開工前要先跟使用者對過版面。**

---

### 階段 A+ — 公司檔案 + 股價

- 公司檔案：`submissions.json`，零額外請求（見上方欄位清單）
- 股價：yfinance，`ticker` vs `^GSPC` vs `^NDX`，近一年報酬與超額報酬 pp
- **價格來源寫成可換源介面**，Vercel 上先驗證 Yahoo 有沒有被擋，被擋就切 stooq

---

### 階段 B — 13F 對沖基金動向

13F-HR information table 是 XML，無需解 HTML、無需 LLM、數字可驗。
需要準備追蹤基金的 CIK 名冊（參考站追 36 家）。

**呈現時必須標明兩件事，否則會誤導：**13F 有 45 天延遲；只揭露多頭部位。

---

### 階段 B+ — Form 4 內部人買賣 + 高管名單

見上方「已驗證的事實」。`P` vs `10b5-1 的 S` 分流是這階段的核心正確性要求。

---

### 階段 C / D — 需要先改 CLAUDE.md

使用者已同意改規則，但**改動草案要先給他確認，不可偷改**。涉及：

- 硬規則 #3「三大報表數字一律走 companyfacts，不解析 10-K/10-Q HTML」
  → 要開一個新例外：**敘述性段落**（CAMs / MD&A / Item 1 / Item 1A）可解 HTML，
  但**數字一律不得從 HTML 取**
- 硬規則 #5「不做 job queue / worker」
  → 8-K 指引要事件驅動輪詢。注意 **Vercel Hobby 的 cron 最短是一天一次，不是一分鐘**

**CAMs 抽取的已知坑（外部建議沒講到的）：**
1. `Critical Audit Matter` 這串字在報告裡至少出現三次：定義樣板段
   （"Critical audit matters are matters arising from the current period audit..."）、
   實際 CAM 標題、否定句（"We determined that there are no critical audit matters"）。
   全文搜第一個匹配會撈到定義段
2. 判 SRC/EGC 豁免**不能看匹配次數是否為 0** —— 豁免的公司也會印定義樣板。要看否定句
3. 一份 10-K 常有**兩份審計報告**（財報 + 內控 ICFR），CAMs 只在財報那份；
   還可能有前任會計師報告。「向後找簽名」會跨錯報告
4. 終點錨點 `We have served as the Company's auditor since` 是真的
   （PCAOB AS 3101 強制揭露審計任期），可用

**MD&A：**先切 `PART I` / `PART II`，再在 PART II 內找 Item 7，比單純錨點距離穩。

**8-K 指引：**`index.json` 真的存在，但 `Description` 欄位是申報人自填，
**大量為空或只寫 "EX-99.1"**，靠 Description 比對會失敗。開工前先抓 50 份統計空值率。

---

### 階段 E — 台股

**不是延伸，是另一個專案。** TWSE / 公開資訊觀測站有 XBRL（IFRS 分類）但沒有 SEC 那種
穩定免費 API，MOPS 是 form-post HTML，中文科目對照要另做一套。
唯一能共用的是**輸出層**（Excel / 摘要頁），資料層整個另建。

---

## 待討論

- [ ] 摘要頁 UI 版面（使用者指定要另外討論）
- [ ] 13F 追蹤基金名冊要收哪幾家
- [ ] `axis_dropped` 的 7 家（WMT 部分交叉表型態）：留白 vs `verified=false`
- [ ] `period_hole`（54 個訊號）：需要兩種報導結構在同一期共存才解得掉
- [ ] `config/segment_axes.json` 小修：BW 地區成員 `ID` / `PH` → 印尼 / 菲律賓
