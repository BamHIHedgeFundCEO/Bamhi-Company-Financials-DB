# CLAUDE.md

完整規格見 `docs/CLAUDE_CODE_PROMPT.md`（唯一真相來源）。以下為開發時的硬規則摘要。

## 絕對不要做

1. 不在後端下載財報檔案 — 只回傳 SEC 官方 URL
   　例外：**公司簡介分頁的敘述性段落**（Item 1 業務／Item 1A 風險／Item 7 MD&A）
   　需要年報 HTML 本文，走 `web/server/utils/narrative.ts`。三道護欄：
   　① 只抓**年報的主文件**，以申報書號為 key 永久快取（已申報不可變）。
   　　　公司簡介本身只抓最新那一份；**逐年對比**（`narrative-diff.get.ts`）要多抓
   　　　前一年那份，所以拆成**獨立端點、點了才載** —— 打開公司簡介的人不必為
   　　　對比付那一次請求，而且兩份 24MB 的 HTML 不會落在同一個請求裡
   　② 單檔 24MB 上限，超過就放棄該公司並在頁面說明（避免 serverless OOM）
   　③ 抽出來的是**散文，不是檔案**：不落地存檔、不轉檔、不提供下載，
   　　　使用者要原始檔仍然只給 SEC 官方 URL
2. 不做 PDF 轉檔（無 playwright / pdfkit / headless chromium）
3. 三大報表數字一律走 `companyfacts` API，不解析 10-K/10-Q HTML
   　例外：**分部數據**。`companyfacts` 不含維度（dimension），分部數字只在申報的
   　XBRL instance（結構化 XML，不是 HTML）裡，走 `web/server/utils/segments.ts`
   　同理，**多股別公司的期末股數與加權平均股數**（波克夏／Visa／ADT… 按股別拆、帶維度）
   　也只在 instance 裡；但三大報表有 ≤2 次 SEC 請求的硬限制，所以走離線預算：
   　`tools/class_shares.py` → `config/class_shares.json`，執行期零 SEC 請求。
   　新的一季要重跑該工具，沒跑到的期維持 n/a（誠實留白，不是錯的數字）
   　**分割事件的仲裁**（`arbitrateSplits`）會讀雅虎 chart API 的 `events=split`，
   　但這不違反本條：三大報表的**數字**一律仍是 companyfacts 的，雅虎只參與
   　「有沒有發生過一次需要正規化的分割」這個判斷，不提供任何數值，屬呈現層正規化。
   　與股價共用 `getPrices` 的同一個請求與快取，零額外外部請求；抓不到就退回
   　偵測器的原始結果（＝仲裁上線前的行為），不會時有時無
   　例外：**敘述性段落可以解析 10-K HTML，但數字一律不得從 HTML 取**。
   　`narrative.ts` 抽出來的文字只做兩件事：原文顯示、附 EDGAR 連結。它不轉數值、
   　不進任何計算、不進 Excel、不進圖表。三大報表與所有指標仍然 100% 來自
   　companyfacts。這條界線是本例外能成立的唯一理由，破了就等於把「數字一定正確」
   　這個承諾交給正規表示式
   　**內部人買賣（Form 3/4/5）不屬於本條例外**：那是結構化 XML（單檔 2–4KB），
   　與分部資料同性質，走 `web/server/utils/insider.ts`
4. 不做登入 / 帳號 / 訂閱 / 付費牆
5. 不做 zip 打包 / job queue / Redis / worker（v2 才加）
6. `filings.recent` 只含最近約 1000 筆，更早的要抓 `filings.files` 分頁
7. 不接 Google Sheets API
8. Excel 一律伺服器端生成；**用 Python + openpyxl（excel-service/），不是 exceljs**（exceljs 無法寫原生圖表）

## SEC 合規

- 每個 sec.gov / data.sec.gov 請求帶 `User-Agent: BamHI <營運者email>`（環境變數 `SEC_USER_AGENT`），否則 403
- 限速走 `web/server/utils/rateLimiter.ts` 獨立模組（100ms 最小間隔 + 429 指數退避）；v2 換 Redis 只改此檔實作
- 三大報表：單 ticker 查詢 ≤ 2 次 SEC 請求
- 分部數據：每份申報 2 次（`index.json` 定位 instance + 抓 instance）。預設 2 份年報
  + 4 份 10-Q = 12 次。份數能壓這麼低是因為申報自帶比較期：一份 10-K 帶 3 個年度、
  一份 10-Q 帶當季與去年同季 → 約 4–6 個年度 + 7–8 個季度。已申報財報不可變，
  解析結果永久快取，12 次是每家公司一次性成本
- 公司簡介：submissions（與其他端點共用快取）+ 最新年報 HTML 1 次。每家一次性成本
- 逐年對比：再 + 前一年的年報 HTML 1 次（點了才載，永久快取）。
  **前一份年報常常不在 `filings.recent` 裡**：JPM 一年發幾百份 424B2，那 1000 筆連
  一整年都蓋不滿 —— 實測 recent 裡只有一份 10-K。要走 `filings.files` 分頁（`getFilings`），
  視窗只開到上一份年報前後約 500 天，免得多翻幾頁就多幾次請求
- 內部人買賣：submissions + 每份 Form 3/4/5 一次，預設 30 份、UI 可調到 120。
  單檔 2–4KB。**兩層快取**：逐份永久快取 + 整包快取（key 綁最新一份申報書號）。
  只有逐份的話每次仍要 30–120 次讀取、限速 100ms 序列 → 實測 3.2 秒；
  綁書號之後沒有新申報就 1 次讀取回應（實測 4.7 秒 → 3 毫秒）
- 回應快取：行程內 Map（`secFetch.ts`）+ 持久層 Vercel Blob（`blobCache.ts`）。
  已提交財報不可變 → 永久有效。**只快取解析後的小結果，不快取原始 instance**
  （單檔 0.7–14MB，存原始檔會把 Blob 額度吃光）

## 設定層（規模化判準）

改 `config/xbrl_zh_map.json`（科目/翻譯/指標）、`config/chart_spec.json`（圖表）、`config/theme.json`（配色/格式）、`config/segment_axes.json`（分部軸/成員正規化/中文名）就能完成的需求，禁止改程式碼實現。

## Excel 硬規則（excel-service/）

- 分頁：說明 / 損益表 / 資產負債表 / 現金流量表 / 關鍵指標 / 估值倍數 / 情景估值 /
  分部數據 / 原始資料（估值倍數、情景估值、分部數據為條件式：無股價或該公司未揭露分部
  時不產生；情景估值與估值倍數同進退，它要引用後者的 EV/EBITDA 歷史中位數與股價輸入格，
  **必須排在它後面建**）
- **流量科目的 TTM 不能寫死「近四欄」**。外國發行人（20-F，`periodicity: 'annual'`）
  一欄就是一整年，加四欄等於四年 —— P/E 變成實際的四分之一，而且看起來完全正常
  （SHEL FY2025 顯示 4.26 倍，實際 22.81）。`valuation.ts` 的 `ttm` 與 workbook 的
  `ttm()` 都要分流；PEG 的「去年同期」同理，季度往前四欄、年度往前一欄
- 情景估值分頁＝**唯一一頁輸出不是 SEC 事實**的分頁（樂觀／中性／悲觀三條流水線，
  營收 × 利潤率 → EBITDA × 終端倍數 → EV ＋現金 −有息負債 ＋認股權證履約流入 →
  ÷ 完全稀釋股數 → 機率加權 → 折現）。四條硬規則：
  ① **每個推導格都要擋空輸入**：Excel 把空白當 0，`營收 × 利潤率` 碰到空格不報錯，
  　 會安靜算出 EBITDA 0 → 目標價 $0.00 → 上漲空間 −100%（還套上紅底）。
  　 一律 `IF(COUNT(依賴格)<n,"待輸入",…)` —— COUNT 只數數值，文字與空白都不算，
  　 「待輸入」因此會自己往下游傳遞，不會半路變回 0
  ② **機率合計必須是 100%**：SUMPRODUCT 不會因為機率只加到 80% 就報錯，
  　 它只會給你一個小 20% 的期望值。不等於 1 時加權兩格直接不出數字
  ③ **歷史參考欄不預填進輸入格**：與 n/a 絕不寫 0 同一條規則 —— 預填會讓人把
  　 「拿過去當未來」誤當成有根據的預測。參考欄本身全是公式（引用其他分頁）
  ④ **折現率只有一格、三情境共用**：情境法的風險已經體現在參數與機率上，
  　 對悲觀情境再加折現率是把同一個風險算兩次。折的是股權價值 → 填權益成本非 WACC
- 情景估值的目標年 ＝ **最近一個會計年度期末 + 3 年**，不是最新一季 + 3 年
  （AAPL 最新是 6 月底的 Q3，那樣會落在 2028-06-30，但 AAPL 9 月結 —— 折現年數
  與「FY2028」這個標籤會各說各話）。折現年數走 `YEARFRAC(TODAY(), 目標年期末日)`，
  自己隨時間更新，不寫死
- 認股權證／選擇權的數量與履約價在 10-K 附註本文（HTML），依硬規則數字不得從 HTML 取，
  所以那一列**沒有歷史參考值**、只有輸入格。履約會**同時**增加股數與現金
  （行使 200 萬股、履約價 $11.50 → 股數 +200 萬、現金 +2,300 萬），
  只填股數不填現金，目標價會系統性偏低
- `payload.get("segments", {})` 在 key 存在但值是 None 時回 **None 不是預設值**，
  而 `main.py` 的 pydantic model 一定會把 segments 補成 None → 分部抽取失敗的公司
  整本活頁簿 500。這類存取一律寫 `(payload.get(...) or {})`
- 分部數據分頁的**列必須動態產生**：ASC 280 規定 CODM 看什麼才揭露什麼，各家
  科目不同（Apple 有分部成本、NVDA 有折舊、銀行給稅前損益），寫死列一定會錯
- 調節項（公司未分攤、分部間沖銷）只掛 ConsolidationItemsAxis、**沒有分部軸維度**，
  在 `segDims` 檢查就會被丟掉，`exclude_patterns` 根本輪不到它作用。它們是合併總額的
  一部分（PG：5 分部 86,112M + corporate 919M = 合併 87,032M），抽取時先收進
  `residual`，再用「加進去才對得上總額」反推該補到哪個軸 —— 本來就對得上的軸不能硬塞。
  **只有淨增加對得上的期數才採用**（UNH 補了反而弄壞更多期）
- 分部的上層匯總成員（如 Apple 的「產品」含 iPhone/Mac/iPad）照列但不進合計，
  否則重複計算；判定靠與合併總額對帳，不是硬編碼。取「加總等於總額且成員數最多」
  的那一組當子項，且**年度欄與季度欄各自投票、同一種粒度內一次決定**。兩邊都不能少：
  全部期間一起投票的話，10-Q 的期數多會蓋掉 10-K 多揭露的那一層（ORCL 地區軸年報
  給 3 大區＋5 國家兩層、10-Q 只給 3 大區，7:4 蓋過去 → 年度欄兩層全加變兩倍）；
  逐欄各判則會變成硬湊 —— LLY 每期十幾種藥、容差是總額 0.5%，隨便剔幾個就落進容差，
  實測會讓每個年度欄剔掉不同的藥然後全部「校驗通過」，把真正對不上的事實藏起來。
  合計公式因此是逐欄的（`=SUM(C5:C12)-C7`），不能跨欄共用固定範圍
- 分部期間 key 一律帶 `#A`／`#Q` 後綴。10-Q 的 instance 裡同一個期末日**同時**有
  單季（~90 天）與累計（YTD，177–273 天）兩組事實，只用期末日當 key 會互相覆蓋，
  拿到的是隨機的單季或累計數字（AAPL/KO/TSLA/MSFT 實測皆有此陷阱）
- 分部分頁年度欄在前、季度欄在後，兩段不混畫在同一張圖：年度長條與季度長條並排
  會差四倍高，看起來像業績暴跌。圖表欄範圍只取其中一段，**且逐區塊選段**——
  地區軸多半只有年度揭露（ASC 280 不要求季度），整頁固定畫季度會生出空白圖框
- 分部格子有兩種留白，**不能都寫 n/a**：n/a ＝該揭露卻查不到（讀者要去查），
  「—」＝該期不適用這一列。會不適用有兩種情形：①該軸只有年度揭露；
  ②公司改組報導分部、成員標籤整組換掉（BW 在 FY2025 10-K 把三個分部併成單一
  分部），新舊結構各自成一個區塊、各自合計，標題寫「報導結構 1／2」。
  分代靠成員的期間集合做 union-find：有交集就同一代，代與代之間必然不相交。
  **只是新增或裁撤一個分部不會被切開**——那時舊成員的期間涵蓋新成員，兩者相交。
  分出 4 代以上視為零散揭露而非改組，不切。不適用的欄也不能放 SUM／IFERROR
  公式：SUM 會算出假的 0，IFERROR 會把「—」吞成 n/a，兩種留白又混回同一個符號
- A 欄中文、B 欄英文、C 欄起季度；凍結窗格 `C2`
- 缺值寫 `n/a`，絕不寫 0（SEC 無標籤 ≠ 零）
- Q4 推算值（FY − Q1 − Q2 − Q3）底色淺橘，說明分頁註明
- 數值存原始美元，格式 `#,##0;[Red](#,##0)`；比率 `0.0%`；倍數 `0.00"x"`；天數 `0"天"`
- 關鍵指標分頁**必須寫 Excel 公式**（IFERROR 包除法），不能寫算好的數值
- 每次從零生成整本活頁簿，圖表程式化建立（不用範本檔——重存會遺失圖表）
- 圖表資料範圍依 `n_quarters` 動態計算，不寫死

## 網頁分頁（個股頁的六個分頁）

`財報下載`（原有工具）／`公司簡介`／`財務報表`／`轉折點`／`13F`／`內部人買賣`。

- 損益表大方向的長條走**損益表四層**（營收 → 毛利 → 營業利益 → 淨利），不放營業費用
  ——費用是被扣掉的那一塊，和營收並排要心算才看得懂。沒申報的那層自動消失
- **敘述性章節一律是節錄，不是全文**：每章 ≤14 段（風險章節只給小標 + 導言 6 段），
  完整原文一律導到 EDGAR。整包送出去的話 API 回應會到 178 KB，而且沒人讀得完
- **圖表不用外部函式庫**，純 SVG（`ComboChart.vue`／`WaterfallChart.vue`）。
  長條圖的左軸**一定含 0**（截軸會讓 3% 的差看起來像三倍）；`null` **一律斷線不內插**
- **缺資料的圖整張不畫**，並在頁尾「沒有呈現的圖」列出是哪個科目沒申報。
  半張圖比不畫更糟 —— 讀者會把缺值當成業績下滑。
  哪些公司會少哪張圖：`python tools/chart_coverage.py`（羅素 3000，零 SEC 請求）
- **利潤瀑布的小計取公司申報值**，不用前面幾根加總；對不上的差額以虛線框的
  「其他」畫出來，不得改小計把差額藏掉
- 外國發行人（`periodicity: 'annual'`）的期別是年度：「季增率」在那裡是年增率，
  文案必須隨旗標切換，不能寫死「季」
- 內部人買賣**必須分流**：`P` 與非 10b5-1 的 `S` 才是訊號；`A`／`M`／`F`（薪酬）與
  10b5-1 計畫賣出分開呈現，禁止合併成一個「內部人賣出 $X」
- **轉折點分頁給量化評分，但每一分都要能拆回原始數字**。總分 → 五個構面 → 逐項
  （指標原始值、bad／good 錨點、線性得分、權重）全部攤在頁面上。權重與錨點寫在
  `config/scoring.json`，公式一律**只寫 metric id 不寫公式**——同一條公式兩份設定
  各存一份，改了一邊就會安靜地互相矛盾。頁面固定顯示評分模型版號與覆蓋率，
  改權重就要改版號，否則同一家公司昨天 72 今天 65，沒人知道是公司變了還是我們改了
- **總分走勢不能用 Sparkline 畫**（`ScoreTrend.vue`）。Sparkline 的「y 軸一定含 0」
  是給**變化量**用的規則 —— 0 在那裡是「沒有變化」，有意義。總分是 0–100 的有界指數，
  多數公司整條線落在 40–70，把 0 塞進來之後整段被壓成頂端一條水平線（AAPL 實測
  55 → 64 與 55 → 56 看起來一樣）。但也不能把 y 軸縮到資料範圍，那會把 2 分的抖動
  畫成暴跌。三件事一起做：y 軸固定 0–100（不截軸）＋背景畫**評級級距的帶狀區**
  （走勢的意義是「跨過了哪一條線」）＋**每一點直接標數字**。
  尺寸走 ResizeObserver 量出來的實際像素、viewBox 與它 1:1（與 ComboChart 同一招）：
  固定 viewBox 再用 CSS 撐滿的話字會跟著縮放，桌機上 17px、手機上 5px
- **頁面不寫我們自己的檔名與工具指令**。`config/scoring.json`、`tools/f13.py --live`
  這類字串對讀者沒有意義（他們沒有那個檔案也跑不了那支程式），只會讓人以為漏了一步。
  該講的是**事實本身**：權重與錨點攤在上面那張表裡、索引由離線批次預先建立
- **總分是純水準分，不混趨勢分**。一半的指標本身就是變化量（營收加速度、毛利率
  年變化、應收成長差），再套一層趨勢分＝二階微分，同一件事算兩次；而且把趨勢揉進
  分數會讓「體質好但正在惡化」被平均成中間值，那正是最該被看見的情況。
  方向另外給箭頭：**本期總分 − 去年同期總分**
- **箭頭只能比兩期都算得出來的項目**。直接拿兩期公開總分相減，測到的是資料多寡不是
  公司表現——實測 JPM 會報出 +28 分的假轉好，其實只是上一期少算了幾項
- **評分用的指標一律滾動四季（TTM）**，包含變化型的比較基準。單季對固定門檻打分會把
  季節性讀成體質變化：KO 實測總分逐季在 39～69 之間跳。ROE／ROIC 尤其不能用
  「單季 ×4」年化。改成 TTM 之後 PG／WMT 的相鄰季差中位數是 1 分、AAPL 2 分、KO 2.5 分。
  含 `[t-1]` 的 TTM 指標在年度模式（20-F 一欄＝一整年）整條消失 → 每個計分項
  **必須備 `metric_annual`** 退回單期版本，`tools/check_signals.py` 會擋。
  **退回去的那一條自己也不能含 `[t-1]`**——`revenue_accel` 就是這樣靜靜空了一整年：
  它的公式有 `revenue[t-1]`，於是外國發行人的營收加速度從來沒有值、也沒有任何地方報錯。
  年度版要另寫（`revenue_accel_annual`，用 `[t-4]`／`[t-8]`，年度模式下換算成前 1／2 欄）
- **「算得出來幾項」與「覆蓋率」是兩個數字，頁面不可以用等號串起來**。前者是計分項
  的**個數**（19/26），後者是這些項目佔多少**權重**（69%）—— 每項權重不同，兩者本來
  就不會相等，而總分門檻擋的是權重那個。寫成「19/26 = 69%」會被讀成同一件事，
  然後 15/26＝57.7% 這種對不上的算式會讓人以為數字算錯了
- **「這家公司沒有這個科目」在判定前要先窮盡標籤**。NKE／AZO／SYY／GAP 把整筆存貨
  標成 `InventoryFinishedGoodsNetOfReserves`（他們的存貨本來就幾乎全是成品）、
  DLTR 用 `RetailRelatedInventoryMerchandise`、NKE 的短期投資用 2018 年後改名的
  `DebtSecuritiesAvailableForSaleExcludingAccruedInterestCurrent`。少一個標籤就會讓
  存貨天數、現金轉換循環、存貨成長差、速動比率整組消失，而且顯示成「這門生意沒有存貨」
  —— 那是我們漏標，不是公司沒揭露。NKE 實測覆蓋率 59% → 69%、總分 44 → 51
- **不分流動／非流動的資產負債表上沒有「短期負債」那一行**，所以有息負債一律走
  `debt_total`（`long_term_debt + short_term_debt?`），不要在公式裡寫「短期＋長期」。
  兩項都要有值的舊寫法讓保險、REIT、資產管理**實測 22 家**（AIG／ALL／BLK／BX／PLD／
  WELL…）的負債權益比、負債佔資產、淨負債、ROIC 四個指標整組落空 —— 那不是抓不到，
  是那張表本來就長這樣。羅素 150 實測覆蓋率中位 90.1% → 91.9%、22 家改善
  （BLK 94%→100%、ARE 54.7%→68.0%、AZO 80.1%→93.5%）。
  三件**驗過而不採用**的作法，別再試：
  - **長期那一項不可以也選用**。ADC／BXP／EXR／O 只掛得到商業本票，首項選用會算出
    「有息負債＝商業本票」（ADC 14 億 vs 實際 38.5 億）—— 半套答案比留白糟，它們維持 n/a
  - **不拿 `DebtInstrumentCarryingAmount` 當直接標籤**。它確實是合計（1,239 家重疊期
    ÷（長期＋短期）中位 1.008、76% 在 ±3% 內），但標籤優先於推算，會讓附註的帳面金額
    蓋掉資產負債表上的數字，兩處對不起來看起來像壞掉
  - **分項標籤不可以相加**（`SecuredDebt`＋`SeniorNotes`＋`UnsecuredDebt`＋`LineOfCredit`）：
    對帳中位 1.330、p90 44.95，它們互相重疊
- **要新增同義標籤前先跑重疊期對帳**（`tools/tag_validate.py` 的同一套方法：同一家、
  同一期、兩個標籤都有值的格子逐格比中位數比值與 ±3% 一致率）。實測擋下三個看起來
  很合理的候選：`DirectOperatingCosts` 當銷貨成本（中位 0.619、一致率 14%）、
  `CostsAndExpenses` 當銷貨成本（中位 2.140 —— 那是總成本含 SG&A）、
  `DeferredRevenueNoncurrent` 當合約負債（中位 0.537，它只是其中一段）。
  **「這家公司沒申報」與「我們漏標籤」的差別只有對帳分得出來**，看標籤名字猜一定會錯
- **「覆蓋率低」有四種原因，只有一種是我們的錯**，混在一起看會得出錯的結論。
  148 家實測：ok 80.1%、`inapplicable` 10.4%（公司報表上沒有這一行）、`missing` 7.6%
  （該有卻抓不到＝我們漏標籤）、`not_meaningful` 1.5%（產業上沒定義）、`invalid` 0.4%。
  **追求「每家都 100%」會讓評分變差**：要 100% 就只能留下每家都申報的項目
  （營收成長、淨利率、ROE、負債權益比、稀釋）約 5 項，分不出 NVDA 與一家電力公司。
  鑑別力正好住在那些不是每家都有的項目裡。正確的目標是三個數字：`missing` 趨近 0、
  `inapplicable` 標成「—」而不是 n/a、每家的覆蓋率門檻維持 35%
- **適用性表要跟 `xbrl_zh_map.json` 一起重跑**（`config/company_applicability.json`
  的 `map_version`）。新增科目會改動詞元的 idf，**整張表的判定跟著漂**——
  表停在 v1.22、map 走到 v1.26 的時候，新加的那 15 個產業限定科目（存款、已賺保費、
  投資性不動產…）在表上根本不存在，於是保險經紀（AJG／AON）的綜合成本率、
  非銀行放款業者（AFRM）的存放比全部顯示 n/a ——「該申報卻抓不到」。
  那些公司根本不做那門生意，正確的留白是「—」。
  重跑要四包 DERA 季度 zip（一季只有 10-Q，年報才揭露的行會被誤判成不適用）：
  `python tools/fsds_coverage.py 2025q3.zip … 2026q2.zip --company-applicability config/company_applicability.json`
  羅素 150 實測 `missing` 235 → 220 格、覆蓋率中位 91.1% → 92.1%
- **標籤名裡 `Excluding…` 後面那一段不能進詞元表**（`fsds_coverage.py` 的
  `_before_excluding`，只影響適用性判斷的 vocab，不影響 `tags` 對照取值）。
  那一段講的是**這一行不含什麼**。銷貨成本收了
  `CostOfGoodsAndServiceExcludingDepreciationDepletionAndAmortization`，
  於是 depreciation／depletion／amortization 三個詞進了它的詞元表 ——
  **任何一家把 `DepreciationDepletionAndAmortization` 排在損益表上的公司**
  （航空、公用事業、油氣都這樣排）都會被判成「有銷貨成本這一行」，光這三個詞就
  9.65 分、是門檻 3.0 的三倍。結果是毛利率、現金轉換循環、存貨天數整組寫成 n/a，
  但那些公司的損益表上根本沒有銷貨成本。修掉之後 AEE 覆蓋率 81.0% → 89.1%、
  AWK 78.2% → 86.3%
- **判不出銷貨成本的公司仍然有，這是已知極限不是待修的 bug**。詞元比對分不出
  「成本」與「收入」：`SalesRevenueServicesNet` 對銷貨成本也是 9.65 分（sales／
  revenue／services 三個詞都在銷貨成本的詞元表裡，因為 `CostOfSales`／`CostOfRevenue`／
  `CostOfServices` 都是它的標籤）。美國航空因此從「—」翻回 n/a。
  **翻回 n/a 是安全的方向**（難看但誠實），翻成「—」才是說謊，所以不要為了這幾家
  去調鬆門檻。全市場普查（`--na-gaps`）也確認**沒有安全的標籤可補**：
  公用事業／航空的候選全是折舊分項（`UtilitiesOperatingExpenseDepreciationAndAmortization`、
  `CostOfGoodsAndServicesSoldDepreciation`），拿分項當合計就是被擋掉過的那條路
- **「整列沒有就視為 0」的科目不能標不適用**。兩條規則對同一件事（整個期間都沒申報）
  給互斥的結論：補 0 說「算得出來」，不適用說「整條作廢」——而 `metrics.ts` 的不適用
  是**整個指標**作廢，連算得出來的期別一起清掉。AAON 與 AXTA 沒有短期投資，補 0 之後
  淨負債／EBITDA 與速動比率本來都有值，標上不適用之後兩項一起變「—」，
  覆蓋率 100% → 91.9%。**值都算得出來了還寫「—」是說謊的方向**
- **稅前淨利在近年很多公司不掛合計標籤**（ORCL／COST／PG／ITW／MCD／PYPL 只申報
  `...BeforeIncomeTaxesDomestic`／`Foreign` 的年度附註拆分）→ 整條落空，ROIC 與
  有效稅率跟著死。用 `derive: net_income + income_tax` 回推並標成推算值
- `dep_only`／`amort_only` 曾經是空標籤清單，`dna` 的 `derive` 從來沒有生效過。
  **設定層的空陣列不會報錯，只會讓整條 derive 靜默失效**
- 總額抓不到時用分層科目相加推算（`inventory` 的 `derive`），但**分層全列為選用項、
  首項缺就整條不推算**：只揭露原料而沒有成品的（FCX 近期）維持 n/a，寧可留白也不給
  半套答案。KR（FIFO 減 LIFO 準備）、XOM（能源存貨＋零件兩行）需要減法與跨科目相加，
  目前仍是 n/a
- 推算出來的總存貨會讓**成品佔比恆等於 1、年變化恆等於 0** —— 那是無資訊不是
  「結構沒變化」，會被評分讀成中性分。用 `inventory_other`（總額−成品）當守門員，
  ≤0 就整項作廢
- **缺項移出分母，不是給 0 分**；覆蓋率有**三道門檻，少一道就會出現「看起來正常、
  其實是雜訊」的總分**：構面內部覆蓋率 <40% 則整個構面移出總分（只靠一兩項撐起 25%
  權重的不是「這一層的分數」）；全頁權重覆蓋率 <35% 不給總分；**進總分的構面權重合計
  <60% 也不給總分**。第三道是前兩道補不起來的洞：BLK（資產管理，SIC 6211）整頁覆蓋率
  36.5% 過得了第一道，資金構面靠權益對資產一項（權重 1.5／3.3 ＝ 45%）過得了第二道，
  最後 86 分是用 5 個構面裡的 2 個、16 項裡的 6 項算出來的。羅素 150 實測有 9 家
  （6%）因這道被擋下，全市場 4,385 → 4,074 家。頁面要寫出**是哪一道**擋的，
  「覆蓋率不足」四個字看不出要去補什麼
- **評分構面按產業分七套**（`config/scoring.json` 的 `models`，按 SIC 選，對不到就用
  頂層的通用模型）：通用／銀行 6020–6221／產險 6320–6499／壽險 6310–6319／
  權益型 REIT 6500–6599 + 6798／資產管理與經紀 6280–6289／抵押型 REIT（**沒有 SIC，
  只能由權益型 REIT 的事實分流到達**，見下）。理由是拿通用模型的 26 個計分項
  去套，金融股只有 7～8 項算得出來、覆蓋率 32～35%，全部落在「無法評分」——
  那不是它們體質差，是我們拿錯了尺。44 家金融股實測 18 → 44 家有總分；
  全市場 4,074 → 4,260 家。幾條跟著來的硬規則：
  - **評級級距與三道門檻七套共用**。級距是全市場總分的分位數，每套各訂一組的話
    「穩健」在銀行頁與軟體頁會是兩件事，而讀者只看得到同樣兩個字。
    但**總分只在同一套模型之內可比**，頁面要寫出用的是哪一套
  - **SIC 區間不能一樣寬**（`check_signals.py` 會擋）：`pickModel` 取最窄的一段，
    一樣窄就變成看陣列順序，設定寫錯不報錯只會安靜給另一套分數
  - **壽險要跟產險分開**。壽險的「已賺保費」不含投資型保單躉繳金（存款性質）但
    「給付」含利息給付 → 綜合成本率結構性 >1（實測 PRU 1.87、MET 1.47，用產險的尺
    一律 0 分）；分離帳戶資產（MET 1,570 億、PRU 1,990 億）掛在表上但風險歸保戶，
    算資本厚度要扣掉（`equity_to_general_assets`）
  - **2 位大類有時候不是同業**。SIC 63 把壽險（6311）與產險（6331）裝在同一桶，
    權益對資產差五倍 → 這類項目標 `peer4_only`，只認 4 位 SIC，查不到寧可退回絕對錨點
  - **絕對錨點也要有出處**。壽險那一套的 bad／good 取自 DERA 裡 SIC 6311 自己的
    p10／p90（各項 12–21 家，不足 30 家所以進不了 `peer_stats.json`），逐項寫
    `anchor_note` 標明筆數並顯示在頁面上——錨點哪來的也是「每一分都拆得回去」的一環
  - **銀行的「營收」要自己定義**。us-gaap 的 `Revenues` 對銀行沒有共識、很多銀行
    根本不申報 → 同業錨點算出 net_margin 的 p90 = 333%。改用 `bank_revenue`
    （淨利息收入＋非利息收入，與效率比同一個底）
  - **拿不到的就留白，不用近似值冒充**：逾放比、備抵覆蓋率、CET1、RBC、FFO／AFFO、
    同店 NOI、出租率全部只在附註表格或監理申報，companyfacts 沒有，而數字不得從 HTML 取
  - **SIC 分不出來的時候，用事實分**（`fallback_if_absent`）。SIC 6200／6211（券商）與
    6141／6159（消費金融）裡各住著兩種生意。判準是**有存款 或 放款佔資產 ≥25%**：
    兩者之一成立就是放款業者走銀行模型，兩者都不成立的（貝萊德、盈透、Robinhood、LPL）
    靠手續費過活，走資產管理模型。**兩個條件缺一不可**，兩個方向都被實測打過臉：
    只看存款 → 不吸收存款的放款業者（Affirm 放款佔資產 60.5%、Credit Acceptance 134.6%、
    OneMain 90.5%）被丟進資產管理模型，整個資產品質構面消失；
    只看放款 → LPL 給招募顧問的過渡貸款（`NotesReceivableNet`）把它留在銀行模型、
    覆蓋率 33% 拿不到總分。而放款**不能單獨當必要條件**：富國銀行只用維度揭露放款，
    companyfacts 抓不到，但它當然是銀行——它有存款。
    門檻 25% 不是拍的：不是放款業者的最高 19.7%（LPL），是的最低 46.5%（Raymond James），
    中間是空的。換過模型一定要在頁面上寫出**為什麼換**（含實際算出來的佔比）
  - **資產管理沒有 AUM**。`AssetsUnderManagementCarryingAmount` 十家實測只有一家申報
    無維度的值，其餘都在 MD&A 表格或帶維度的附註裡，而數字不得從 HTML 取 →
    這一套沒有 AUM 成長、也沒有費率（營收 ÷ AUM）。改用薪酬佔比（業內的 comp ratio）
    當成本結構的主項，但它只有一半的公司抓得到（`LaborAndRelatedExpense`
    十家只有五家無維度申報）→ 同一層再配一個營業費用率墊底，否則這個構面
    會在一半的公司身上整個消失
  - **私募與另類（BX／APO／KKR）的淨利與營運現金流被合併基金的未實現損益主導**，
    與傳統資產管理公司不是同一種波動。同業錨點（SIC 6282）把兩者放在一起，
    頁面要寫出這一點
  - **抵押型 REIT 要跟權益型分開**，判準是**投資性不動產這個科目在不在**
    （`real_estate`，`RealEstateInvestmentPropertyNet`／`...AtCost`）。它不收租金、
    沒有物業、沒有出租率也沒有 FFO，本質是一支用附買回融資的槓桿債券組合；
    用權益型那 14 項去套，AGNC 只有 4 項算得出來（覆蓋率 23.6%）。
    實測 16 家權益型（O／PLD／WELL／SPG／AVB／EQR／KIM／REG…）全部申報該科目，
    AGNC 與 NLY 一個都沒有；RITM／STWD 有（真的持有物業），留在權益型是對的 ——
    **混合型不硬切，判準只問一件事實**。12 家實測覆蓋率 0–56% → 全部拿得到總分
    （PMT 0.0% → 86.4%）。這一套只有三個構面（利差與成本／槓桿與帳面價值／配息），
    不是刪減版的權益型模型
  - **事實分流只看最近四期，不是「有沒有任何一期有值」**。Annaly 的商用不動產部門
    2021 年賣掉，最後一筆投資性不動產停在 2020-12-31 —— 足以讓它繼續套權益型模型、
    覆蓋率 23.6% 拿不到總分。分流問的是「這家公司**現在**做的是哪一門生意」
  - **SIC 6798 這一格的同業錨點不能用**（`absolute_only`）。`peer_stats.json` 只認 SIC，
    分不出權益型與抵押型，而權益對資產實測權益型 35–45%、抵押型中位 11.8% ——
    共用一把尺的話抵押型一律 0 分。抵押型那一套七項全部 `absolute_only`，
    錨點取**這 11 家自己的分布**，逐項寫 `anchor_note` 標明筆數。
    `peer4_only` 是「別退大類」，這一條是「連 4 位 SIC 都別查」，兩者解的是同一類問題
  - **小樣本不能直接取 p10／p90 當錨點**。n=11 的兩端被極端值主導（稀釋率 p90 是
    97.6%，DX 一年股數接近翻倍），中位數會映射到 65–86 分，10 家的總分中位是 72 ——
    而評級級距是全市場分位數、七套共用，這樣「穩健」在這一頁就不是同一件事。
    改成**以中位數為中心、半寬取較窄的那一側** min(中位−p10, p90−中位)，
    中位數固定映射到 50 分，實測總分中位 72 → 62（全市場中位 58）
- **產業限定的科目與指標帶 `sector` 旗標**（`config/xbrl_zh_map.json`）：存款、放款、
  已賺保費、分離帳戶資產這些是評分的輸入，但**不該出現在製造業的資產負債表上**，
  也不該讓每個人的 Excel 關鍵指標分頁多出永遠 n/a 的列。旗標的科目與指標
  **不進 lineItems／derived**，改由 `sectorItems`／`sectorDerived` 送給 `/api/signals`，
  `financials.get.ts` 在回應前刪掉這兩欄 → 三大報表、Excel、CSV 的形狀完全不變
  （AAPL 實測仍是 58 列科目 + 91 個指標）。與 `internal` 的差別：internal 連指標都
  看不到（它只是推算的中繼）
- **同業錨點的樣本要跟執行期採計的格子一致**。`peer_stats.py` 原本沒套
  `invalid_if_nonpositive` 與 `not_meaningful_sic` → 分布裡混進了淨利為負時的現金含量
  （一律是漂亮的負數），把 bad 端整個往下拉：REIT 的 `ocf_to_net_income_ttm` p10
  實測 −3.24，而執行期根本不會採計那種格子。套上守門員之後是 1.11
- **產業上沒有定義的項目要明確排除**（`not_meaningful_sic` ＋ 一定要寫 note）。
  金融業的自由現金流、營運現金流對淨利比、總資產週轉率屬於這一類：那不是「表現差」，
  是這個指標對那門生意不存在。讓它算出一個難看的分數等於評分在說謊
- **同一個指標不可以在評分裡出現兩次**（`check_signals.py` 會擋）。週轉天數與應收／
  存貨背離講的是同一件事，分在兩個構面會把同一個問題扣兩次分
- 錨點兩層：**同業百分位優先**（`config/peer_stats.json`，`tools/peer_stats.py` 從
  SEC DERA 季度資料集離線算，先 4 位 SIC、樣本不足退 2 位大類），查不到才用
  `scoring.json` 的跨產業絕對值。公式與程式完全不動，只換 bad／good 兩個數字。
  **逐項要標出用的是哪一層**（頁面上的「同業／同業大類／絕對」）——
  錨點哪來的也是「每一分都拆得回去」的一環
- **百分位的寬窄要量過再訂**。p25／p75 實測 27.6% 的格子飽和在 100 分，
  等於四分之一的項目分不出高下；p05／p95 又把總分壓在 41–74、標準差剩 6.6。
  取 **p10／p90**：飽和 4.8%／13.2%，標準差 7.4
- **換錨點就要一起重訂評級級距**。級距原本照絕對錨點的尺度訂（強健 ≥80），
  換成同業百分位之後全市場總分中位數是 53、p90 是 69 → 沒有人拿得到「強健」，
  評級靜靜失去意義。`--score-dist` 用同一套彙總規則跑全市場（4,385 家算得出來、
  1,739 家覆蓋率不足）再用分位數訂：底 10%／10–30%／30–70%／70–90%／頂 10%
- **同業錨點量的是「相對於同業」，不是「好不好」**。整個產業一起惡化時分數不會反映，
  產業普遍虧損時虧損公司也會落在中段（RIVN 實測 60 分）。頁面要寫出來
- **`peer_stats.py` 是迷你公式語言的第三份實作**（TS 給網頁、`formulas.py` 給 Excel）。
  三份不一致同一個指標會給三種數字而且不報錯 → 上線前要對帳：實測
  `deferred_revenue_yoy`／`dilution_rate`／`equity_yoy`／`ocf_to_net_income_ttm`
  四個年度型指標與 `/api/signals` 逐位相同
- 評分旁邊**必須有免責聲明**，且不能只放頁尾：權重與錨點是我們設定的、只用 SEC 數字、
  不含產業前景與股價、不是投資建議
- 轉折訊號與評分**零額外 SEC 請求**：`/api/signals` 走 `getFinancials` 同一條路、同一層快取，
  指標公式取自 `xbrl_zh_map.json` 的 `derived`（與 Excel 關鍵指標分頁同一份）。
  `web/server/utils/metrics.ts` 是 `excel-service/formulas.py` 的 TS 對照實作，
  **兩邊語意必須一致**（`[t-4]`／`avg()`／年度模式的 ×4→×1、91.25→365），
  不一致的話同一個指標在網頁與 Excel 給不同數字，而且沒有任何地方會報錯
- **現金流品質一律用滾動四季，不用單季**。單季的營運現金流被期末的一收一放主導：
  NVDA 的營運現金流對淨利比實測在 0.40～1.46 之間逐季彈跳，拿單季當轉折會得到
  隨機的結論（同一家公司 TTM 是 0.70）。滾動四季的公式含 `[t-1]`，在年度模式
  （20-F 一欄＝一整年）整條無意義 → 那種訊號**必須備 `metric_annual`**
  指到同名的單期指標，否則外國發行人整格空白。`tools/check_signals.py` 會擋
- **比值的分母為負就整格作廢**（`invalid_if_nonpositive`）：淨利為負時的現金含量、
  EBITDA 為負時的淨負債／EBITDA 會算出漂亮的負數而「看起來很安全」，那是反的。
  **負債權益比也要擋**（分母是股東權益）：長年買回自家股票的公司權益是負的，
  AZO 實測權益 −27.85 億、有息負債 90.16 億 → D/E ＝ −3.24，而錨點是「越低越好」
  → 直接飽和到 100 分，抱著 90 億負債的公司在償債構面拿滿分。羅素 150 裡
  AAL／ABBV／AZO／BBWI／BKNG／BRBR／CAH／CAR 八家中招。
  **新的計分項只要是比值，第一件事就是問分母會不會為負**
- **單一格惡化不算轉折**。應收成長差與應收天數、存貨成長差與存貨天數、現金含量與
  應收/存貨互為印證，兩個同時亮才在卡片上標「互相印證」；單期為正常常只是大單
  集中在期末出貨。跨產業必然有例外的（資本支出強度、商譽佔比、股東權益年增率）
  一律標 `state_override: info`，只給背景不判好壞
- 轉折點分頁的留白有**三種**，不能混：n/a（該申報卻抓不到）／—（科目對這家公司
  不存在，或比較基期落在所選期間之外）／作廢（分母為負）。每一種在卡片上都要寫出
  是哪一種、為什麼
- 13F 走**離線批次**（`tools/f13.py` → `config/f13/`，一季重跑一次），執行期零 SEC 請求。
  七件做錯就會生出假數字的事：只收 `13F-HR`（`13F-NT` 沒有持股表）；修正案要分
  `RESTATEMENT`（整份取代）與 `NEW HOLDINGS`（與原申報相加）；`PUTCALL` 非空是選擇權、
  `SSHPRNAMTTYPE != SH` 是債券本金，都不計入股數；**持有人一律以 CIK 為 key**
  —— 申報人自己填的名稱連大小寫都會變（PNC 這季改成全大寫），名稱進 key 會憑空
  生出一筆清倉加一筆建倉；另外三件：
- 13F 的**修正案一定要等所有來源收齊之後才解析一次**，不能一包一包各自解析再合併。
  原申報與它的 `NEW HOLDINGS` 補充申報常常分屬不同批次（實測 43 組），逐包解析的話
  那一包只看得到補充申報、把它當成整份持股，合併時再被 `setdefault` 定住 →
  那家的持股憑空少一大截
- **「本期沒有申報」不是清倉**。遲交、或改交 `13F-NT`（持股由母公司代報）的機構，
  原始資料上就是「上期有、本期無」。實測 VMC 的 147 筆清倉裡 19 筆（13%）、
  107 筆建倉裡 22 筆（21%）是這樣來的。判準是硬事實（那家那一期到底有沒有交過
  任何一份 13F），不是名稱推測，所以**連家數一起扣掉**、另列「尚未申報」區
  —— 這點與重組配對相反，重組只移出排行榜、不動家數
- **保密處理的殘缺申報要整份丟掉**：`SUMMARYPAGE.ISCONFIDENTIALOMITTED = Y`
  且揭露列數 < 自報 `TABLEENTRYTOTAL` 的一半。Norges Bank 每年 Q1／Q3 只交一份
  1 列的 13F-HR（自報 1,507 檔），滿一年後才補完整修正案 → 不處理的話它會在 Q1
  假清倉、Q2 假建倉，2026Q2 光 AAPL 就是 1.9 億股的假建倉。丟掉時**連「他有交過」
  都不算**，那家就自動落進「尚未申報」區。門檻不是猜的：9,846 份申報裡宣告保密的
  99 份中，揭露 ≥10 列而不到自報一半的**一份都沒有**，50% 落在空白地帶。
  自報總數不能單獨當判準（填錯的很多），JPMorgan 原申報只有 378 列是送件出錯、
  他們自己補了 RESTATEMENT 全表，修正案機制已處理
- **13F 批次資料集會落後整整一季**，必須用 `--live` 補。SEC 的 zip 不是一季一包，
  是滾動三個月的**申報視窗**（`01mar2026-31may2026` ＝申報日落在該區間者）。
  2026Q2（期別 `30-JUN-2026`）截止日 8/14，落在下一包裡而那包 9 月才發布 →
  每年有四段各約一個月，只靠資料集的話網站停在上一季，別的網站都已經換季。
  實例：TCI Fund Management 2026Q2 建倉 VMC 2,447,004 股（$721.9M），2026Q1 沒有這檔。
  `--live` 改走 `full-index/{年}/QTR{季}/form.idx` 逐份抓送件檔：一季約 9,500 份、
  gzip 後僅 ~60MB、0.15 秒限速約 25 分鐘，解析結果永久快取（已申報不可變）
- **13F 的母體是 SEC 掛牌公司清單，不是羅素 3000**。財報頁對任何查得到的代號都能用，
  13F 卻只有羅素 3000 有資料的話，BW（287 家機構持有）、TSM（4,856 家）、ASML
  在頁面上會變成「沒有機構持股紀錄」。7,026 檔、26.6 MB
- **一個代號要收下所有對到的 CUSIP**。公司重新註冊會換號，兩季之間就斷掉 ——
  Carnival 2026Q2 從 `143658300` 換成 `G2004J103`，只認新號的話上一季只剩 27 家、
  945 筆假建倉。而且**不能取「最新交割失敗檔的代號」當答案**：CUSIP 退場時 NSCC 會
  把代號改寫成 `HONZZZZ`／`TRIXXXX`／`6205REGWAY`，那正好就是換過號的那幾檔。
  作法是收下看過的所有代號，**只採用母體裡有的**，退場代號自動被濾掉
- **分割／反向分割／股票股利一定要正規化**，否則「沒有人買賣但每個人股數都變了」
  會變成全體增持或全體減持：KLAC 1,919 家增持對 9 家減持、HON 1,889 家減持對 95 家。
  偵測用兩期都持有的人的股數比：中位數偏離 1 超過 2%，且**緊密度**（落在中位數
  ±0.5% 的人佔比）≥12% 且兩期都持有 ≥100 家。實測真的那 14 檔是 13.3–36.3%、
  雜訊最高 11.4%，中間是空的。倍數**一律取眾數**：中位數會被零股拉歪
  （CUE 中位 0.0449、實際 1:30），而雅虎對**分拆**回報的是價格調整因子不是股數比
  （HON 回 0.9535、實際股數比 0.5000）。雅虎只用來替小樣本背書「這一季有同方向的事件」
- **只合併股數基準相同的 CUSIP**。反向分割會換號，同一期裡少數幾家還在報舊號、
  用的是分割前的股數，直接相加股數會被灌爆（CUE 舊號每股 $0.55、新號 $31.56）。
  判準用每股價格（申報市值 ÷ 申報股數），以申報家數最多的號為準，
  價差超過 25% 的整筆不計入 —— **不做換算**，換算等於自己造數字
- **家數的兩條恆等式一定要成立**，個股頁也要把算式寫出來讓人當場核：
  `本季 = 增持+減持+不變+建倉+尚未申報(進)+重組(進)`、
  `上季 = 增持+減持+不變+清倉+尚未申報(出)+重組(出)`。
  重組配對是**多對一**，`reorgs`（配對數＝移入數）不等於移出數，
  上季那條要用 `reorgsOut` —— 用配對數會有 582 檔對不起來
- **分割正規化之後不能再用「完全相等」判斷持股不變**：倍數是 1/3、1.0371 這種
  非整數的話浮點相乘不會剛好等於原值，沒動的人會全部被誤分到增持或減持
  （DD 與 PHG 的「持股不變」直接變成 0 家）。給 0.2% 的相對容差
- **排行榜的排序鍵一定要是表格裡看得見的一欄**。「建倉·大型」按比例排、畫面上
  只有建倉家數（250 → 696 → 387）的時候，看起來就像沒排序
- **規模是篩選器不是更多張榜**：批次只寫原始列（`_leaders.json`），排序移到
  `f13leaders.get.ts` 吃 `?size=`。先算好的話 12 張榜 × 4 級距要存 48 份，
  改門檻還得重跑整批。規模用**機構申報持股市值**分段（≥$100 億／$10–100 億／<$10 億），
  不是公司市值 —— `dei:EntityCommonStockSharesOutstanding` 不可靠，
  companyfacts 裡 Visa 最新的一筆是 2010 年的
- **分拆上市不能算建倉**：HONA 上季 0 家、本季 2,055 家，那是股東收到股票。
  所有排行榜要求上季至少 20 家持有，這一類另立「新上市／分拆」榜
- 13F 的「申報主體重組」要單獨標示：Vanguard 2026Q1 把 `VANGUARD GROUP INC` 拆成
  六個新 CIK，原始資料上就是 14.3 億股 AAPL 清倉 + 9.5 億股建倉，但沒有人真的買賣過。
  配對到的移出排行榜、另列一區，**但家數統計不動**（配對是靠名稱推測的，
  不能讓推測值去改真正的數字）
- **逐年對比比的是小標，不是全文**（`narrativeDiff.ts`）。風險因子的每一條小標本身
  就是一句完整的風險敘述，新增一條風險就是多一個小標——這是分析師本來就在做的事。
  散文的逐字 diff 會被改寫與數字更新淹沒，等於什麼都沒說；而數字本來就不該從這裡讀。
  六條讓它不說謊的規則：
  - **小標清單有 120 條上限**（`narrative.ts` 的 `slice(0, 120)`）。碰到上限時，
    尾端那幾條在「去年有今年沒有」與「超過上限沒收進來」之間分不出來 →
    `capped` 一律清空新增／刪除清單，只留數量摘要並在頁面寫明原因
  - **兩年小標數量差 50% 以上要標 `asymmetric`**。那是「抽取結果不一樣」的指紋：
    TSM 的 20-F 實測今年 56 條、去年 21 條，35 條全算新增、刪除 0 條——
    公司不會一年之間多寫 35 條風險又一條都沒刪。清單照列但先說「可能來自抽取」
  - **段落層級只在兩年抽到同一個小節時才比**。`paragraphs` 是節錄（風險 6 段、
    其他 14 段），節錄取哪幾段由 focus 小節決定 → 兩年取到不同小節時，
    逐段比對出來的「新增」其實是我們自己換了取樣點（TSLA 實測
    「Overview and 2025 Highlights」對上「Overview and 2024 Highlights」）
  - **表格家具要濾掉**：`Year Ended December 31, 2024`、`% Change from Prior Period`
    （GOOGL）、`Total Firm 318,512 Total Firm 318,512`、`Shares Amount Shares Amount`
    （表格被剝成一行＝同一串字重複兩次）。它們每年都變，不濾的話每年穩定生出
    一條假新增加一條假刪除
  - **配對要看開頭的實詞**，不能只靠詞集合相似度。律師改寫風險小標幾乎都是從中段
    開始改：JPM 的「JPMorganChase's businesses are highly regulated and are…」對上
    去年的「…, and the laws, rules and regulations that apply to…」詞集合只有 0.31，
    會被報成「新增一條＋刪除一條」。開頭連續 4 個實詞相同就視為同一條（改寫）
  - **不判斷變動是好是壞**，顏色也不能用 `--pos`／`--neg`（那兩個在本站是
    「轉好／警訊」的語意色）。新增一條風險不代表公司變差，可能只是把既有的風險
    拆成兩條；刪掉一條也不代表那個風險消失了
- 敘述章節的粗體記號**必須是 `\s` 匹配得到的空白**（U+000B）。Alphabet 的標題長成
  `ITEM 1.<粗體>BUSINESS`，記號卡在中間 —— 用 `` 之類的控制字元當記號的話，
  `item\s*1\s*\.?\s*business` 永遠比對不到，GOOGL／TSLA／META／AMZN 四家的
  三個章節會全部落空而且不會報錯
- 年報一律**優先取原始 10-K，不取 10-K/A**。修正案多半只重申被修正的那幾項
  （常常只有 Part III），裡面沒有 Item 1／1A／7。TSLA 2026 年 4 月補了一份 10-K/A，
  照申報日取最新就會挑到它
- 敘述段落的繁中翻譯走**離線批次**（`tools/translate_narrative.py` →
  `config/narrative_zh/{cik}-{accession}.json`），執行期零成本、可人工覆核。
  譯文檔**綁申報書號**：公司出新年報就是新書號 → 舊譯文自動失效、頁面退回英文，
  不會拿去年的翻譯套今年的財報。逐項對齊，沒翻到的那一條顯示英文原文並標「原文」。
  **數字永遠不經過翻譯層**
- 翻譯引擎預設走**本機 Ollama**（`--ollama`，免費、免帳號、資料不出機器，
  實測約 0.6 秒/條）；`--api` 走 Anthropic API 但要付費，不是必要路徑
- **數字一律不經過翻譯層，而且要有守門員驗算**。模型會自己做單位換算，
  而且常常差一到兩個數量級：`$224 million` → 「224 億美元」（100 倍）、
  `$951.7 million` → 「95.17 億美元」（10 倍）。實測 2,724 個帶單位的金額裡
  **24.2% 換算錯誤**，錯了不報錯、讀者也查不出來（散文旁邊沒有能對帳的東西）。
  兩層處理：prompt 要求 million→百萬／billion→十億／thousand→千、數字本身不動；
  `numeric_gate` 譯完逐條驗算量值，對不上就帶著錯在哪重譯一次，還是不對就留空
  （頁面顯示英文原文）。**prompt 只是請求，守門員才是保證**。
  守門員的三個細節少一個，誤報就會蓋過真錯：中文複合數詞要加起來
  （「2億1800萬」＝2e8＋1.8e7）、英文單位字原樣留著（「€704.3 million」）是對的行為、
  沒帶單位字又小於 1000 的不驗（日期的 31、Q3 寫成「第三季」會一直誤報）
- `fill()` **依索引續跑，不是依長度**。守門員擋下的那條清成空字串留在原位，
  只看 `len(done)` 的話它永遠不會被重譯；砍掉則後面每一條都錯位
- 批次的熔斷器**不能丟 `SystemExit`**：`run_batch` 對每一家包了 `except SystemExit`
  吸收單家錯誤，會把熔斷一起接走 —— 實測「已中止」印了 230 次然後照樣跑完。
  用專用的 `BatchAbort` 並在 handler 裡 re-raise。
  而且**熔斷的判準要用事實不能用猜的**：「連續 N 家抽不到章節」可能是母體尾巴
  真的沒有 10-K，也可能是網站暫時抽不出來卻回報成「無年報」。碰到門檻就去問
  一家一定有年報的公司（AAPL），它正常就繼續、它也抽不到才中止。
  猜錯過一次：252 家被記成「無年報」，事後重查 60 家**全部都有年報**
- 簡繁正規化只能用 opencc 的 **`s2t`（逐字）**，**不可用 `s2twp`**。`s2twp` 帶詞彙表，
  套在本來就是繁體的文字上會二次轉換 —— 實測把模型正確產出的「演算法」變成
  「演演算法」（NVDA 那份出現 5 次）。台灣用語改用手工檢查過的對照表，
  每條都必須冪等（套兩次結果不變）

## Edge cases（必須全數處理）

1. Q4 無 10-Q（全年 = 3×10-Q + 1×10-K）
2. Q4 單季數字 = FY − Q1 − Q2 − Q3，標註推算
2b. **累計鏈斷了要先找直接申報的單季事實，再放棄**。單季金額是「兩端累計相減」，
   任一端補出來的就整格作廢 —— 但**斷點後面那一季的單季金額常常就寫在申報書上**。
   ALNY 實測：companyfacts 裡整個 2026Q1 不存在（連資產負債表都沒有，這是 SEC 上游的
   缺口，±12 天內比對全市場 2,595 家有 41 家如此、1.6%），於是 2026Q2 明明有
   2026-04-01→06-30 的 12.91 億直接申報值卻跟著變 n/a。撿回來是「用申報值」不是新的推算，
   10 家缺口公司實測 n/a 2,134 → 2,095 格。
   **Q4 不走這條**：10-K 常把全年金額掛在 Q4 的期間上（L3Harris 連兩年），
   擋它的 `misTagged` 靠「大於前三季累計」比對，累計鏈斷了就無從比對
2c. **期末日在未來的事實一律不收**。已申報的財報不會有未來的期末日，那種事實來自
   **負債到期表**：PennyMac 把可轉債的到期日（2027-03-31…2028-06-30，共 52 筆）
   掛在 `ConvertibleNotesPayable` 上，期間清單因此長出 FY2027 Q2–Q4 三個幽靈季，
   而評分取最新一期 → 整頁 0 項算得出來、覆蓋率 0.0%。界線用「今天」不用
   「最後一次申報日」：後者在剛換季時會把剛發布的那份財報砍掉
3. `filings.recent` 之外的舊申報走 `filings.files` 分頁
4. 外國發行人（TSM、ASML）無 10-Q → 自動改抓 20-F/6-K 並在 UI 告知
5. 10-K/A、10-Q/A 一併列出並標記「修正版」
6. 會計年度非曆年（NVDA、AAPL）→ 一律以 `reportDate` 判斷季別
7. 同一期間多筆 frame → 取 `filed` 最新
8. ticker 不存在 / 下市 / ETF → 明確錯誤訊息
9. 同 CIK 多 ticker（GOOG/GOOGL）→ 雙向解析
10. 控股公司重組（2026 的 XOM）→ ticker 被指到全新 CIK，該實體沒有任何年報、
    companyfacts 只剩 94 個科目 / 2 年（前身有 438 / 18 年）。端點一律走
    `resolveCompany()`（不是 `resolveTicker()`），偵測到就改指前身 CIK。
    判定要兩個條件都成立：現行 CIK 無年報，且候選 CIK 有年報並同時列有同一個
    申報書號 —— 申報書號前綴常是申報代理商，只憑前綴會認錯

## 上限與文案

- 單次 5 檔 ticker、40 季；超限訊息：「單次比較上限 5 檔，可分批查詢」，不用錯誤代碼

## 驗收

AAPL 5年 = 15×10-Q + 5×10-K；TSM 判外國發行人；NVDA 季別對齊 1 月結算；BRK.A 可解析；AAPL/MSFT/TSLA 營收 fallback 皆取值；單查詢 SEC 請求 ≤ 2。
